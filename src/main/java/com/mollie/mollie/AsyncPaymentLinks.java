/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie;

import static com.mollie.mollie.operations.Operations.AsyncRequestOperation;

import com.mollie.mollie.models.operations.CreatePaymentLinkRequest;
import com.mollie.mollie.models.operations.CreatePaymentLinkRequestBody;
import com.mollie.mollie.models.operations.DeletePaymentLinkRequest;
import com.mollie.mollie.models.operations.DeletePaymentLinkRequestBody;
import com.mollie.mollie.models.operations.GetPaymentLinkPaymentsRequest;
import com.mollie.mollie.models.operations.GetPaymentLinkRequest;
import com.mollie.mollie.models.operations.ListPaymentLinksRequest;
import com.mollie.mollie.models.operations.UpdatePaymentLinkRequest;
import com.mollie.mollie.models.operations.UpdatePaymentLinkRequestBody;
import com.mollie.mollie.models.operations.async.CreatePaymentLinkRequestBuilder;
import com.mollie.mollie.models.operations.async.CreatePaymentLinkResponse;
import com.mollie.mollie.models.operations.async.DeletePaymentLinkRequestBuilder;
import com.mollie.mollie.models.operations.async.DeletePaymentLinkResponse;
import com.mollie.mollie.models.operations.async.GetPaymentLinkPaymentsRequestBuilder;
import com.mollie.mollie.models.operations.async.GetPaymentLinkPaymentsResponse;
import com.mollie.mollie.models.operations.async.GetPaymentLinkRequestBuilder;
import com.mollie.mollie.models.operations.async.GetPaymentLinkResponse;
import com.mollie.mollie.models.operations.async.ListPaymentLinksRequestBuilder;
import com.mollie.mollie.models.operations.async.ListPaymentLinksResponse;
import com.mollie.mollie.models.operations.async.UpdatePaymentLinkRequestBuilder;
import com.mollie.mollie.models.operations.async.UpdatePaymentLinkResponse;
import com.mollie.mollie.operations.CreatePaymentLink;
import com.mollie.mollie.operations.DeletePaymentLink;
import com.mollie.mollie.operations.GetPaymentLink;
import com.mollie.mollie.operations.GetPaymentLinkPayments;
import com.mollie.mollie.operations.ListPaymentLinks;
import com.mollie.mollie.operations.UpdatePaymentLink;
import com.mollie.mollie.utils.Headers;
import com.mollie.mollie.utils.Options;
import java.lang.Boolean;
import java.lang.Long;
import java.lang.String;
import java.util.Optional;
import java.util.concurrent.CompletableFuture;
import org.openapitools.jackson.nullable.JsonNullable;


public class AsyncPaymentLinks {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final PaymentLinks syncSDK;

    AsyncPaymentLinks(PaymentLinks syncSDK, SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.syncSDK = syncSDK;
    }

    /**
     * Switches to the sync SDK.
     * 
     * @return The sync SDK
     */
    public PaymentLinks sync() {
        return syncSDK;
    }


    /**
     * Create payment link
     * 
     * <p>With the Payment links API you can generate payment links that by default, unlike regular payments,
     * do not expire.
     * The payment link can be shared with your customers and will redirect them to them the payment page
     * where they can
     * complete the payment. A [payment](get-payment) will only be created once the customer initiates the
     * payment.
     * 
     * @return The async call builder
     */
    public CreatePaymentLinkRequestBuilder create() {
        return new CreatePaymentLinkRequestBuilder(sdkConfiguration);
    }

    /**
     * Create payment link
     * 
     * <p>With the Payment links API you can generate payment links that by default, unlike regular payments,
     * do not expire.
     * The payment link can be shared with your customers and will redirect them to them the payment page
     * where they can
     * complete the payment. A [payment](get-payment) will only be created once the customer initiates the
     * payment.
     * 
     * @return {@code CompletableFuture<CreatePaymentLinkResponse>} - The async response
     */
    public CompletableFuture<CreatePaymentLinkResponse> createDirect() {
        return create(Optional.empty(), Optional.empty(), Optional.empty());
    }

    /**
     * Create payment link
     * 
     * <p>With the Payment links API you can generate payment links that by default, unlike regular payments,
     * do not expire.
     * The payment link can be shared with your customers and will redirect them to them the payment page
     * where they can
     * complete the payment. A [payment](get-payment) will only be created once the customer initiates the
     * payment.
     * 
     * @param idempotencyKey A unique key to ensure idempotent requests. This key should be a UUID v4 string.
     * @param requestBody 
     * @param options additional options
     * @return {@code CompletableFuture<CreatePaymentLinkResponse>} - The async response
     */
    public CompletableFuture<CreatePaymentLinkResponse> create(
            Optional<String> idempotencyKey, Optional<? extends CreatePaymentLinkRequestBody> requestBody,
            Optional<Options> options) {
        CreatePaymentLinkRequest request =
            CreatePaymentLinkRequest
                .builder()
                .idempotencyKey(idempotencyKey)
                .requestBody(requestBody)
                .build();
        AsyncRequestOperation<CreatePaymentLinkRequest, CreatePaymentLinkResponse> operation
              = new CreatePaymentLink.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * List payment links
     * 
     * <p>Retrieve a list of all payment links.
     * 
     * <p>The results are paginated.
     * 
     * @return The async call builder
     */
    public ListPaymentLinksRequestBuilder list() {
        return new ListPaymentLinksRequestBuilder(sdkConfiguration);
    }

    /**
     * List payment links
     * 
     * <p>Retrieve a list of all payment links.
     * 
     * <p>The results are paginated.
     * 
     * @return {@code CompletableFuture<ListPaymentLinksResponse>} - The async response
     */
    public CompletableFuture<ListPaymentLinksResponse> listDirect() {
        return list(
                Optional.empty(), JsonNullable.undefined(), Optional.empty(),
                Optional.empty(), Optional.empty());
    }

    /**
     * List payment links
     * 
     * <p>Retrieve a list of all payment links.
     * 
     * <p>The results are paginated.
     * 
     * @param from Provide an ID to start the result set from the item with the given ID and onwards. This allows you to paginate the
     *         result set.
     * @param limit The maximum number of items to return. Defaults to 50 items.
     * @param testmode Most API credentials are specifically created for either live mode or test mode. In those cases the `testmode` query
     *         parameter must not be sent. For organization-level credentials such as OAuth access tokens, you can enable test mode by
     *         setting the `testmode` query parameter to `true`.
     *         
     *         Test entities cannot be retrieved when the endpoint is set to live mode, and vice versa.
     * @param idempotencyKey A unique key to ensure idempotent requests. This key should be a UUID v4 string.
     * @param options additional options
     * @return {@code CompletableFuture<ListPaymentLinksResponse>} - The async response
     */
    public CompletableFuture<ListPaymentLinksResponse> list(
            Optional<String> from, JsonNullable<Long> limit,
            Optional<Boolean> testmode, Optional<String> idempotencyKey,
            Optional<Options> options) {
        ListPaymentLinksRequest request =
            ListPaymentLinksRequest
                .builder()
                .from(from)
                .limit(limit)
                .testmode(testmode)
                .idempotencyKey(idempotencyKey)
                .build();
        AsyncRequestOperation<ListPaymentLinksRequest, ListPaymentLinksResponse> operation
              = new ListPaymentLinks.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Get payment link
     * 
     * <p>Retrieve a single payment link by its ID.
     * 
     * @return The async call builder
     */
    public GetPaymentLinkRequestBuilder get() {
        return new GetPaymentLinkRequestBuilder(sdkConfiguration);
    }

    /**
     * Get payment link
     * 
     * <p>Retrieve a single payment link by its ID.
     * 
     * @param paymentLinkId Provide the ID of the related payment link.
     * @return {@code CompletableFuture<GetPaymentLinkResponse>} - The async response
     */
    public CompletableFuture<GetPaymentLinkResponse> get(String paymentLinkId) {
        return get(
                paymentLinkId, Optional.empty(), Optional.empty(),
                Optional.empty());
    }

    /**
     * Get payment link
     * 
     * <p>Retrieve a single payment link by its ID.
     * 
     * @param paymentLinkId Provide the ID of the related payment link.
     * @param testmode Most API credentials are specifically created for either live mode or test mode. In those cases the `testmode` query
     *         parameter must not be sent. For organization-level credentials such as OAuth access tokens, you can enable test mode by
     *         setting the `testmode` query parameter to `true`.
     *         
     *         Test entities cannot be retrieved when the endpoint is set to live mode, and vice versa.
     * @param idempotencyKey A unique key to ensure idempotent requests. This key should be a UUID v4 string.
     * @param options additional options
     * @return {@code CompletableFuture<GetPaymentLinkResponse>} - The async response
     */
    public CompletableFuture<GetPaymentLinkResponse> get(
            String paymentLinkId, Optional<Boolean> testmode,
            Optional<String> idempotencyKey, Optional<Options> options) {
        GetPaymentLinkRequest request =
            GetPaymentLinkRequest
                .builder()
                .paymentLinkId(paymentLinkId)
                .testmode(testmode)
                .idempotencyKey(idempotencyKey)
                .build();
        AsyncRequestOperation<GetPaymentLinkRequest, GetPaymentLinkResponse> operation
              = new GetPaymentLink.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Update payment link
     * 
     * <p>Certain details of an existing payment link can be updated.
     * 
     * @return The async call builder
     */
    public UpdatePaymentLinkRequestBuilder update() {
        return new UpdatePaymentLinkRequestBuilder(sdkConfiguration);
    }

    /**
     * Update payment link
     * 
     * <p>Certain details of an existing payment link can be updated.
     * 
     * @param paymentLinkId Provide the ID of the related payment link.
     * @return {@code CompletableFuture<UpdatePaymentLinkResponse>} - The async response
     */
    public CompletableFuture<UpdatePaymentLinkResponse> update(String paymentLinkId) {
        return update(
                paymentLinkId, Optional.empty(), Optional.empty(),
                Optional.empty());
    }

    /**
     * Update payment link
     * 
     * <p>Certain details of an existing payment link can be updated.
     * 
     * @param paymentLinkId Provide the ID of the related payment link.
     * @param idempotencyKey A unique key to ensure idempotent requests. This key should be a UUID v4 string.
     * @param requestBody 
     * @param options additional options
     * @return {@code CompletableFuture<UpdatePaymentLinkResponse>} - The async response
     */
    public CompletableFuture<UpdatePaymentLinkResponse> update(
            String paymentLinkId, Optional<String> idempotencyKey,
            Optional<? extends UpdatePaymentLinkRequestBody> requestBody, Optional<Options> options) {
        UpdatePaymentLinkRequest request =
            UpdatePaymentLinkRequest
                .builder()
                .paymentLinkId(paymentLinkId)
                .idempotencyKey(idempotencyKey)
                .requestBody(requestBody)
                .build();
        AsyncRequestOperation<UpdatePaymentLinkRequest, UpdatePaymentLinkResponse> operation
              = new UpdatePaymentLink.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Delete payment link
     * 
     * <p>Payment links which have not been opened and no payments have been made yet can be deleted entirely.
     * This can be useful for removing payment links that have been incorrectly configured or that are no
     * longer relevant.
     * 
     * <p>Once deleted, the payment link will no longer show up in the API or Mollie dashboard.
     * 
     * <p>To simply disable a payment link without fully deleting it, you can use the `archived` parameter on
     * the
     * [Update payment link](update-payment-link) endpoint instead.
     * 
     * @return The async call builder
     */
    public DeletePaymentLinkRequestBuilder delete() {
        return new DeletePaymentLinkRequestBuilder(sdkConfiguration);
    }

    /**
     * Delete payment link
     * 
     * <p>Payment links which have not been opened and no payments have been made yet can be deleted entirely.
     * This can be useful for removing payment links that have been incorrectly configured or that are no
     * longer relevant.
     * 
     * <p>Once deleted, the payment link will no longer show up in the API or Mollie dashboard.
     * 
     * <p>To simply disable a payment link without fully deleting it, you can use the `archived` parameter on
     * the
     * [Update payment link](update-payment-link) endpoint instead.
     * 
     * @param paymentLinkId Provide the ID of the related payment link.
     * @return {@code CompletableFuture<DeletePaymentLinkResponse>} - The async response
     */
    public CompletableFuture<DeletePaymentLinkResponse> delete(String paymentLinkId) {
        return delete(
                paymentLinkId, Optional.empty(), Optional.empty(),
                Optional.empty());
    }

    /**
     * Delete payment link
     * 
     * <p>Payment links which have not been opened and no payments have been made yet can be deleted entirely.
     * This can be useful for removing payment links that have been incorrectly configured or that are no
     * longer relevant.
     * 
     * <p>Once deleted, the payment link will no longer show up in the API or Mollie dashboard.
     * 
     * <p>To simply disable a payment link without fully deleting it, you can use the `archived` parameter on
     * the
     * [Update payment link](update-payment-link) endpoint instead.
     * 
     * @param paymentLinkId Provide the ID of the related payment link.
     * @param idempotencyKey A unique key to ensure idempotent requests. This key should be a UUID v4 string.
     * @param requestBody 
     * @param options additional options
     * @return {@code CompletableFuture<DeletePaymentLinkResponse>} - The async response
     */
    public CompletableFuture<DeletePaymentLinkResponse> delete(
            String paymentLinkId, Optional<String> idempotencyKey,
            Optional<? extends DeletePaymentLinkRequestBody> requestBody, Optional<Options> options) {
        DeletePaymentLinkRequest request =
            DeletePaymentLinkRequest
                .builder()
                .paymentLinkId(paymentLinkId)
                .idempotencyKey(idempotencyKey)
                .requestBody(requestBody)
                .build();
        AsyncRequestOperation<DeletePaymentLinkRequest, DeletePaymentLinkResponse> operation
              = new DeletePaymentLink.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Get payment link payments
     * 
     * <p>Retrieve the list of payments for a specific payment link.
     * 
     * <p>The results are paginated.
     * 
     * @return The async call builder
     */
    public GetPaymentLinkPaymentsRequestBuilder listPayments() {
        return new GetPaymentLinkPaymentsRequestBuilder(sdkConfiguration);
    }

    /**
     * Get payment link payments
     * 
     * <p>Retrieve the list of payments for a specific payment link.
     * 
     * <p>The results are paginated.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return {@code CompletableFuture<GetPaymentLinkPaymentsResponse>} - The async response
     */
    public CompletableFuture<GetPaymentLinkPaymentsResponse> listPayments(GetPaymentLinkPaymentsRequest request) {
        return listPayments(request, Optional.empty());
    }

    /**
     * Get payment link payments
     * 
     * <p>Retrieve the list of payments for a specific payment link.
     * 
     * <p>The results are paginated.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return {@code CompletableFuture<GetPaymentLinkPaymentsResponse>} - The async response
     */
    public CompletableFuture<GetPaymentLinkPaymentsResponse> listPayments(GetPaymentLinkPaymentsRequest request, Optional<Options> options) {
        AsyncRequestOperation<GetPaymentLinkPaymentsRequest, GetPaymentLinkPaymentsResponse> operation
              = new GetPaymentLinkPayments.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }

}
