/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie;

import static com.mollie.mollie.operations.Operations.AsyncRequestOperation;

import com.mollie.mollie.models.components.EntityMandate;
import com.mollie.mollie.models.operations.CreateMandateRequest;
import com.mollie.mollie.models.operations.GetMandateRequest;
import com.mollie.mollie.models.operations.ListMandatesRequest;
import com.mollie.mollie.models.operations.RevokeMandateRequest;
import com.mollie.mollie.models.operations.RevokeMandateRequestBody;
import com.mollie.mollie.models.operations.async.CreateMandateRequestBuilder;
import com.mollie.mollie.models.operations.async.CreateMandateResponse;
import com.mollie.mollie.models.operations.async.GetMandateRequestBuilder;
import com.mollie.mollie.models.operations.async.GetMandateResponse;
import com.mollie.mollie.models.operations.async.ListMandatesRequestBuilder;
import com.mollie.mollie.models.operations.async.ListMandatesResponse;
import com.mollie.mollie.models.operations.async.RevokeMandateRequestBuilder;
import com.mollie.mollie.models.operations.async.RevokeMandateResponse;
import com.mollie.mollie.operations.CreateMandate;
import com.mollie.mollie.operations.GetMandate;
import com.mollie.mollie.operations.ListMandates;
import com.mollie.mollie.operations.RevokeMandate;
import com.mollie.mollie.utils.Headers;
import com.mollie.mollie.utils.Options;
import java.lang.Boolean;
import java.lang.String;
import java.util.Optional;
import java.util.concurrent.CompletableFuture;
import org.openapitools.jackson.nullable.JsonNullable;


public class AsyncMandates {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final Mandates syncSDK;

    AsyncMandates(Mandates syncSDK, SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.syncSDK = syncSDK;
    }

    /**
     * Switches to the sync SDK.
     * 
     * @return The sync SDK
     */
    public Mandates sync() {
        return syncSDK;
    }


    /**
     * Create mandate
     * 
     * <p>Create a mandate for a specific customer. Mandates allow you to charge a customer's card, PayPal
     * account or bank
     * account recurrently.
     * 
     * <p>It is only possible to create mandates for IBANs and PayPal billing agreements with this endpoint.
     * To create
     * mandates for cards, your customers need to perform a 'first payment' with their card.
     * 
     * @return The async call builder
     */
    public CreateMandateRequestBuilder create() {
        return new CreateMandateRequestBuilder(sdkConfiguration);
    }

    /**
     * Create mandate
     * 
     * <p>Create a mandate for a specific customer. Mandates allow you to charge a customer's card, PayPal
     * account or bank
     * account recurrently.
     * 
     * <p>It is only possible to create mandates for IBANs and PayPal billing agreements with this endpoint.
     * To create
     * mandates for cards, your customers need to perform a 'first payment' with their card.
     * 
     * @param customerId Provide the ID of the related customer.
     * @return {@code CompletableFuture<CreateMandateResponse>} - The async response
     */
    public CompletableFuture<CreateMandateResponse> create(String customerId) {
        return create(
                customerId, Optional.empty(), Optional.empty(),
                Optional.empty());
    }

    /**
     * Create mandate
     * 
     * <p>Create a mandate for a specific customer. Mandates allow you to charge a customer's card, PayPal
     * account or bank
     * account recurrently.
     * 
     * <p>It is only possible to create mandates for IBANs and PayPal billing agreements with this endpoint.
     * To create
     * mandates for cards, your customers need to perform a 'first payment' with their card.
     * 
     * @param customerId Provide the ID of the related customer.
     * @param idempotencyKey A unique key to ensure idempotent requests. This key should be a UUID v4 string.
     * @param entityMandate 
     * @param options additional options
     * @return {@code CompletableFuture<CreateMandateResponse>} - The async response
     */
    public CompletableFuture<CreateMandateResponse> create(
            String customerId, Optional<String> idempotencyKey,
            Optional<? extends EntityMandate> entityMandate, Optional<Options> options) {
        CreateMandateRequest request =
            CreateMandateRequest
                .builder()
                .customerId(customerId)
                .idempotencyKey(idempotencyKey)
                .entityMandate(entityMandate)
                .build();
        AsyncRequestOperation<CreateMandateRequest, CreateMandateResponse> operation
              = new CreateMandate.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * List mandates
     * 
     * <p>Retrieve a list of all mandates.
     * 
     * <p>The results are paginated.
     * 
     * @return The async call builder
     */
    public ListMandatesRequestBuilder list() {
        return new ListMandatesRequestBuilder(sdkConfiguration);
    }

    /**
     * List mandates
     * 
     * <p>Retrieve a list of all mandates.
     * 
     * <p>The results are paginated.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return {@code CompletableFuture<ListMandatesResponse>} - The async response
     */
    public CompletableFuture<ListMandatesResponse> list(ListMandatesRequest request) {
        return list(request, Optional.empty());
    }

    /**
     * List mandates
     * 
     * <p>Retrieve a list of all mandates.
     * 
     * <p>The results are paginated.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return {@code CompletableFuture<ListMandatesResponse>} - The async response
     */
    public CompletableFuture<ListMandatesResponse> list(ListMandatesRequest request, Optional<Options> options) {
        AsyncRequestOperation<ListMandatesRequest, ListMandatesResponse> operation
              = new ListMandates.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Get mandate
     * 
     * <p>Retrieve a single mandate by its ID. Depending on the type of mandate, the object will contain the
     * customer's bank
     * account details, card details, or PayPal account details.
     * 
     * @return The async call builder
     */
    public GetMandateRequestBuilder get() {
        return new GetMandateRequestBuilder(sdkConfiguration);
    }

    /**
     * Get mandate
     * 
     * <p>Retrieve a single mandate by its ID. Depending on the type of mandate, the object will contain the
     * customer's bank
     * account details, card details, or PayPal account details.
     * 
     * @param customerId Provide the ID of the related customer.
     * @param mandateId Provide the ID of the related mandate.
     * @return {@code CompletableFuture<GetMandateResponse>} - The async response
     */
    public CompletableFuture<GetMandateResponse> get(String customerId, String mandateId) {
        return get(
                customerId, mandateId, JsonNullable.undefined(),
                Optional.empty(), Optional.empty());
    }

    /**
     * Get mandate
     * 
     * <p>Retrieve a single mandate by its ID. Depending on the type of mandate, the object will contain the
     * customer's bank
     * account details, card details, or PayPal account details.
     * 
     * @param customerId Provide the ID of the related customer.
     * @param mandateId Provide the ID of the related mandate.
     * @param testmode Most API credentials are specifically created for either live mode or test mode. In those cases the `testmode` query
     *         parameter can be omitted. For organization-level credentials such as OAuth access tokens, you can enable test mode by
     *         setting the `testmode` query parameter to `true`.
     *         
     *         Test entities cannot be retrieved when the endpoint is set to live mode, and vice versa.
     * @param idempotencyKey A unique key to ensure idempotent requests. This key should be a UUID v4 string.
     * @param options additional options
     * @return {@code CompletableFuture<GetMandateResponse>} - The async response
     */
    public CompletableFuture<GetMandateResponse> get(
            String customerId, String mandateId,
            JsonNullable<Boolean> testmode, Optional<String> idempotencyKey,
            Optional<Options> options) {
        GetMandateRequest request =
            GetMandateRequest
                .builder()
                .customerId(customerId)
                .mandateId(mandateId)
                .testmode(testmode)
                .idempotencyKey(idempotencyKey)
                .build();
        AsyncRequestOperation<GetMandateRequest, GetMandateResponse> operation
              = new GetMandate.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Revoke mandate
     * 
     * <p>Revoke a customer's mandate. You will no longer be able to charge the customer's bank account or
     * card with this
     * mandate, and all connected subscriptions will be canceled.
     * 
     * @return The async call builder
     */
    public RevokeMandateRequestBuilder revoke() {
        return new RevokeMandateRequestBuilder(sdkConfiguration);
    }

    /**
     * Revoke mandate
     * 
     * <p>Revoke a customer's mandate. You will no longer be able to charge the customer's bank account or
     * card with this
     * mandate, and all connected subscriptions will be canceled.
     * 
     * @param customerId Provide the ID of the related customer.
     * @param mandateId Provide the ID of the related mandate.
     * @return {@code CompletableFuture<RevokeMandateResponse>} - The async response
     */
    public CompletableFuture<RevokeMandateResponse> revoke(String customerId, String mandateId) {
        return revoke(
                customerId, mandateId, Optional.empty(),
                Optional.empty(), Optional.empty());
    }

    /**
     * Revoke mandate
     * 
     * <p>Revoke a customer's mandate. You will no longer be able to charge the customer's bank account or
     * card with this
     * mandate, and all connected subscriptions will be canceled.
     * 
     * @param customerId Provide the ID of the related customer.
     * @param mandateId Provide the ID of the related mandate.
     * @param idempotencyKey A unique key to ensure idempotent requests. This key should be a UUID v4 string.
     * @param requestBody 
     * @param options additional options
     * @return {@code CompletableFuture<RevokeMandateResponse>} - The async response
     */
    public CompletableFuture<RevokeMandateResponse> revoke(
            String customerId, String mandateId,
            Optional<String> idempotencyKey, Optional<? extends RevokeMandateRequestBody> requestBody,
            Optional<Options> options) {
        RevokeMandateRequest request =
            RevokeMandateRequest
                .builder()
                .customerId(customerId)
                .mandateId(mandateId)
                .idempotencyKey(idempotencyKey)
                .requestBody(requestBody)
                .build();
        AsyncRequestOperation<RevokeMandateRequest, RevokeMandateResponse> operation
              = new RevokeMandate.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }

}
