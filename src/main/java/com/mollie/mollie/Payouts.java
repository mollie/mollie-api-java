/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: cd74e182f207
 */
package com.mollie.mollie;

import static com.mollie.mollie.operations.Operations.RequestOperation;

import com.mollie.mollie.models.components.PayoutRequest;
import com.mollie.mollie.models.operations.CancelPayoutRequest;
import com.mollie.mollie.models.operations.CancelPayoutRequestBuilder;
import com.mollie.mollie.models.operations.CancelPayoutResponse;
import com.mollie.mollie.models.operations.CreatePayoutRequest;
import com.mollie.mollie.models.operations.CreatePayoutRequestBuilder;
import com.mollie.mollie.models.operations.CreatePayoutResponse;
import com.mollie.mollie.models.operations.GetPayoutRequest;
import com.mollie.mollie.models.operations.GetPayoutRequestBuilder;
import com.mollie.mollie.models.operations.GetPayoutResponse;
import com.mollie.mollie.models.operations.ListPayoutsRequest;
import com.mollie.mollie.models.operations.ListPayoutsRequestBuilder;
import com.mollie.mollie.models.operations.ListPayoutsResponse;
import com.mollie.mollie.operations.CancelPayout;
import com.mollie.mollie.operations.CreatePayout;
import com.mollie.mollie.operations.GetPayout;
import com.mollie.mollie.operations.ListPayouts;
import com.mollie.mollie.utils.Headers;
import com.mollie.mollie.utils.Options;
import java.lang.Boolean;
import java.lang.String;
import java.util.Optional;


public class Payouts {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncPayouts asyncSDK;

    Payouts(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncPayouts(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncPayouts async() {
        return asyncSDK;
    }

    /**
     * Create payout
     * 
     * <p>Request a payout from one of your balances to the balance's configured bank account.
     * 
     * <p>The payout will be executed on the next scheduled business day. If no `amount` is specified, the
     * full available
     * balance minus any configured balance reserve is paid out.
     * 
     * <p>Once the payout is created with status `requested`, you can cancel it via the
     * [Cancel payout](cancel-payout) endpoint, up until the payout moves to `initiated`.
     * 
     * <p>Creating a payout via the API automatically sets the balance's `transferFrequency` to `never`,
     * pausing any previously configured automatic settlement schedule. To resume automatic settlements,
     * update the transfer frequency via the dashboard.
     * 
     * <p>### Webhooks
     * 
     * <p>Subscribe to the following webhook events to track payout status changes. See the
     * [Webhook Subscriptions API](list-webhooks) for details on subscribing.
     * 
     * <p>| Event | Description |
     * |---|---|
     * | `payout.initiated` | The payout is being executed and funds are reserved. |
     * | `payout.processing-at-bank` | The payout has been submitted to the bank. |
     * | `payout.completed` | The payout has been sent to the destination bank account. |
     * | `payout.canceled` | The payout was canceled via the API before being submitted to the bank. |
     * | `payout.failed` | The payout failed after creation, including bank rejections and post-submission
     * cancellations. |
     * 
     * <p>### Payout failure reasons
     * 
     * <p>A payout request may fail immediately if one of the following conditions applies:
     * 
     * <p>- A payout is already scheduled for the next business day for this balance.
     * - The balance has insufficient funds.
     * - The balance is not active.
     * - Payouts are blocked for this organization.
     * - The balance has queued refunds.
     * - One of the organization's balances is below the negative balance threshold.
     * - The payout destination (bank account) is invalid or not configured.
     * 
     * @return The call builder
     */
    public CreatePayoutRequestBuilder create() {
        return new CreatePayoutRequestBuilder(sdkConfiguration);
    }

    /**
     * Create payout
     * 
     * <p>Request a payout from one of your balances to the balance's configured bank account.
     * 
     * <p>The payout will be executed on the next scheduled business day. If no `amount` is specified, the
     * full available
     * balance minus any configured balance reserve is paid out.
     * 
     * <p>Once the payout is created with status `requested`, you can cancel it via the
     * [Cancel payout](cancel-payout) endpoint, up until the payout moves to `initiated`.
     * 
     * <p>Creating a payout via the API automatically sets the balance's `transferFrequency` to `never`,
     * pausing any previously configured automatic settlement schedule. To resume automatic settlements,
     * update the transfer frequency via the dashboard.
     * 
     * <p>### Webhooks
     * 
     * <p>Subscribe to the following webhook events to track payout status changes. See the
     * [Webhook Subscriptions API](list-webhooks) for details on subscribing.
     * 
     * <p>| Event | Description |
     * |---|---|
     * | `payout.initiated` | The payout is being executed and funds are reserved. |
     * | `payout.processing-at-bank` | The payout has been submitted to the bank. |
     * | `payout.completed` | The payout has been sent to the destination bank account. |
     * | `payout.canceled` | The payout was canceled via the API before being submitted to the bank. |
     * | `payout.failed` | The payout failed after creation, including bank rejections and post-submission
     * cancellations. |
     * 
     * <p>### Payout failure reasons
     * 
     * <p>A payout request may fail immediately if one of the following conditions applies:
     * 
     * <p>- A payout is already scheduled for the next business day for this balance.
     * - The balance has insufficient funds.
     * - The balance is not active.
     * - Payouts are blocked for this organization.
     * - The balance has queued refunds.
     * - One of the organization's balances is below the negative balance threshold.
     * - The payout destination (bank account) is invalid or not configured.
     * 
     * @param payoutRequest 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public CreatePayoutResponse create(PayoutRequest payoutRequest) {
        return create(Optional.empty(), Optional.empty(), payoutRequest,
            Optional.empty());
    }

    /**
     * Create payout
     * 
     * <p>Request a payout from one of your balances to the balance's configured bank account.
     * 
     * <p>The payout will be executed on the next scheduled business day. If no `amount` is specified, the
     * full available
     * balance minus any configured balance reserve is paid out.
     * 
     * <p>Once the payout is created with status `requested`, you can cancel it via the
     * [Cancel payout](cancel-payout) endpoint, up until the payout moves to `initiated`.
     * 
     * <p>Creating a payout via the API automatically sets the balance's `transferFrequency` to `never`,
     * pausing any previously configured automatic settlement schedule. To resume automatic settlements,
     * update the transfer frequency via the dashboard.
     * 
     * <p>### Webhooks
     * 
     * <p>Subscribe to the following webhook events to track payout status changes. See the
     * [Webhook Subscriptions API](list-webhooks) for details on subscribing.
     * 
     * <p>| Event | Description |
     * |---|---|
     * | `payout.initiated` | The payout is being executed and funds are reserved. |
     * | `payout.processing-at-bank` | The payout has been submitted to the bank. |
     * | `payout.completed` | The payout has been sent to the destination bank account. |
     * | `payout.canceled` | The payout was canceled via the API before being submitted to the bank. |
     * | `payout.failed` | The payout failed after creation, including bank rejections and post-submission
     * cancellations. |
     * 
     * <p>### Payout failure reasons
     * 
     * <p>A payout request may fail immediately if one of the following conditions applies:
     * 
     * <p>- A payout is already scheduled for the next business day for this balance.
     * - The balance has insufficient funds.
     * - The balance is not active.
     * - Payouts are blocked for this organization.
     * - The balance has queued refunds.
     * - One of the organization's balances is below the negative balance threshold.
     * - The payout destination (bank account) is invalid or not configured.
     * 
     * @param testmode Most API credentials are specifically created for either live mode or test mode. In those cases the `testmode` query
     *         parameter must not be sent. For organization-level credentials such as OAuth access tokens, you can enable test mode by
     *         setting the `testmode` query parameter to `true`.
     *         
     *         Test entities cannot be retrieved when the endpoint is set to live mode, and vice versa.
     * @param idempotencyKey A unique key to ensure idempotent requests. This key should be a UUID v4 string.
     * @param payoutRequest 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public CreatePayoutResponse create(
            Optional<Boolean> testmode, Optional<String> idempotencyKey,
            PayoutRequest payoutRequest, Optional<Options> options) {
        CreatePayoutRequest request =
            CreatePayoutRequest
                .builder()
                .testmode(testmode)
                .idempotencyKey(idempotencyKey)
                .payoutRequest(payoutRequest)
                .build();
        RequestOperation<CreatePayoutRequest, CreatePayoutResponse> operation
              = new CreatePayout.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * List payouts
     * 
     * <p>Retrieve a list of all payouts for your organization, including payouts initiated automatically by
     * the
     * balance's payout schedule and payouts requested via the API or dashboard.
     * 
     * <p>Only payouts created on or after April 1st, 2026 are returned.
     * 
     * <p>The results are paginated. Use the `from` query parameter together with `_links.next` to iterate
     * through
     * the full result set.
     * 
     * @return The call builder
     */
    public ListPayoutsRequestBuilder list() {
        return new ListPayoutsRequestBuilder(sdkConfiguration);
    }

    /**
     * List payouts
     * 
     * <p>Retrieve a list of all payouts for your organization, including payouts initiated automatically by
     * the
     * balance's payout schedule and payouts requested via the API or dashboard.
     * 
     * <p>Only payouts created on or after April 1st, 2026 are returned.
     * 
     * <p>The results are paginated. Use the `from` query parameter together with `_links.next` to iterate
     * through
     * the full result set.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListPayoutsResponse list(ListPayoutsRequest request) {
        return list(request, Optional.empty());
    }

    /**
     * List payouts
     * 
     * <p>Retrieve a list of all payouts for your organization, including payouts initiated automatically by
     * the
     * balance's payout schedule and payouts requested via the API or dashboard.
     * 
     * <p>Only payouts created on or after April 1st, 2026 are returned.
     * 
     * <p>The results are paginated. Use the `from` query parameter together with `_links.next` to iterate
     * through
     * the full result set.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListPayoutsResponse list(ListPayoutsRequest request, Optional<Options> options) {
        ListPayouts.Sync operation
              = new ListPayouts.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Get payout
     * 
     * <p>Retrieve a single payout by its ID.
     * 
     * @return The call builder
     */
    public GetPayoutRequestBuilder get() {
        return new GetPayoutRequestBuilder(sdkConfiguration);
    }

    /**
     * Get payout
     * 
     * <p>Retrieve a single payout by its ID.
     * 
     * @param payoutId Provide the ID of the payout.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetPayoutResponse get(String payoutId) {
        return get(payoutId, Optional.empty(), Optional.empty(),
            Optional.empty());
    }

    /**
     * Get payout
     * 
     * <p>Retrieve a single payout by its ID.
     * 
     * @param payoutId Provide the ID of the payout.
     * @param testmode Most API credentials are specifically created for either live mode or test mode. In those cases the `testmode` query
     *         parameter must not be sent. For organization-level credentials such as OAuth access tokens, you can enable test mode by
     *         setting the `testmode` query parameter to `true`.
     *         
     *         Test entities cannot be retrieved when the endpoint is set to live mode, and vice versa.
     * @param idempotencyKey A unique key to ensure idempotent requests. This key should be a UUID v4 string.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetPayoutResponse get(
            String payoutId, Optional<Boolean> testmode,
            Optional<String> idempotencyKey, Optional<Options> options) {
        GetPayoutRequest request =
            GetPayoutRequest
                .builder()
                .payoutId(payoutId)
                .testmode(testmode)
                .idempotencyKey(idempotencyKey)
                .build();
        RequestOperation<GetPayoutRequest, GetPayoutResponse> operation
              = new GetPayout.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Cancel payout
     * 
     * <p>Cancel a payout. A payout can only be canceled while it has the status `requested`. Once the payout
     * moves
     * to `initiated`, it is too late to cancel.
     * 
     * <p>The canceled payout object is returned with the status set to `canceled`.
     * 
     * @return The call builder
     */
    public CancelPayoutRequestBuilder cancel() {
        return new CancelPayoutRequestBuilder(sdkConfiguration);
    }

    /**
     * Cancel payout
     * 
     * <p>Cancel a payout. A payout can only be canceled while it has the status `requested`. Once the payout
     * moves
     * to `initiated`, it is too late to cancel.
     * 
     * <p>The canceled payout object is returned with the status set to `canceled`.
     * 
     * @param payoutId Provide the ID of the payout.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public CancelPayoutResponse cancel(String payoutId) {
        return cancel(payoutId, Optional.empty(), Optional.empty(),
            Optional.empty());
    }

    /**
     * Cancel payout
     * 
     * <p>Cancel a payout. A payout can only be canceled while it has the status `requested`. Once the payout
     * moves
     * to `initiated`, it is too late to cancel.
     * 
     * <p>The canceled payout object is returned with the status set to `canceled`.
     * 
     * @param payoutId Provide the ID of the payout.
     * @param testmode Most API credentials are specifically created for either live mode or test mode. In those cases the `testmode` query
     *         parameter must not be sent. For organization-level credentials such as OAuth access tokens, you can enable test mode by
     *         setting the `testmode` query parameter to `true`.
     *         
     *         Test entities cannot be retrieved when the endpoint is set to live mode, and vice versa.
     * @param idempotencyKey A unique key to ensure idempotent requests. This key should be a UUID v4 string.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public CancelPayoutResponse cancel(
            String payoutId, Optional<Boolean> testmode,
            Optional<String> idempotencyKey, Optional<Options> options) {
        CancelPayoutRequest request =
            CancelPayoutRequest
                .builder()
                .payoutId(payoutId)
                .testmode(testmode)
                .idempotencyKey(idempotencyKey)
                .build();
        RequestOperation<CancelPayoutRequest, CancelPayoutResponse> operation
              = new CancelPayout.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
