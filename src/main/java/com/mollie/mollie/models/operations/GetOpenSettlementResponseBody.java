/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 6cf14bb5e9e4
 */
package com.mollie.mollie.models.operations;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.mollie.mollie.models.components.OpenSettlementId;
import com.mollie.mollie.models.components.SettlementConvenienceLinks;
import com.mollie.mollie.utils.Utils;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.Map;
import java.util.Optional;
import org.openapitools.jackson.nullable.JsonNullable;

/**
 * GetOpenSettlementResponseBody
 * 
 * <p>A settlement object describing your current balance. For a complete reference of the settlement
 * object, refer to
 * the [Get settlement](get-settlement) endpoint documentation.
 */
public class GetOpenSettlementResponseBody {
    /**
     * Indicates the response contains a settlement object. Will always contain the string `settlement` for
     * this
     * endpoint.
     */
    @JsonProperty("resource")
    private String resource;


    @JsonProperty("id")
    private OpenSettlementId id;

    /**
     * The entity's date and time of creation, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601)
     * format.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("createdAt")
    private Optional<String> createdAt;

    /**
     * The settlement's bank reference, as found in your Mollie account and on your bank statement.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("reference")
    private JsonNullable<String> reference;

    /**
     * The date on which the settlement was settled, in ISO 8601 format.
     * 
     * <p>For an [open settlement](get-open-settlement) or for the [next settlement](get-next-settlement), no
     * settlement
     * date is available.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("settledAt")
    private JsonNullable<String> settledAt;


    @JsonProperty("status")
    private Status status;


    @JsonProperty("amount")
    private Amount amount;

    /**
     * The balance token that the settlement was settled to.
     */
    @JsonProperty("balanceId")
    private String balanceId;

    /**
     * The ID of the oldest invoice created for all the periods, if the invoice has been created yet.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("invoiceId")
    private JsonNullable<String> invoiceId;

    /**
     * For bookkeeping purposes, the settlement includes an overview of transactions included in the
     * settlement. These
     * transactions are grouped into 'period' objects — one for each calendar month.
     * 
     * <p>For example, if a settlement includes funds from 15 April until 4 May, it will include two period
     * objects. One for
     * all transactions processed between 15 April and 30 April, and one for all transactions between 1 May
     * and 4 May.
     * 
     * <p>Period objects are grouped by year, and then by month. So in the above example, the full `periods`
     * collection will
     * look as follows: `{"2024": {"04": {...}, "05": {...&rbrace;&rbrace;}`. The year and month in this documentation
     * are referred as `&lt;year&gt;` and `&lt;month&gt;`.
     * 
     * <p>The example response should give a good idea of what this looks like in practise.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("periods")
    private Optional<? extends Map<String, Map<String, Periods>>> periods;

    /**
     * An object with several relevant URLs. Every URL object will contain an `href` and a `type` field.
     * 
     * <p>This endpoint always points to your organization's current open or next settlement rather than one
     * specific
     * settlement, so it doesn't return links to that settlement's payments, captures, refunds,
     * chargebacks, or invoice.
     */
    @JsonProperty("_links")
    private SettlementConvenienceLinks links;

    @JsonCreator
    public GetOpenSettlementResponseBody(
            @JsonProperty("resource") String resource,
            @JsonProperty("id") OpenSettlementId id,
            @JsonProperty("createdAt") Optional<String> createdAt,
            @JsonProperty("reference") JsonNullable<String> reference,
            @JsonProperty("settledAt") JsonNullable<String> settledAt,
            @JsonProperty("status") Status status,
            @JsonProperty("amount") Amount amount,
            @JsonProperty("balanceId") String balanceId,
            @JsonProperty("invoiceId") JsonNullable<String> invoiceId,
            @JsonProperty("periods") Optional<? extends Map<String, Map<String, Periods>>> periods,
            @JsonProperty("_links") SettlementConvenienceLinks links) {
        Utils.checkNotNull(resource, "resource");
        Utils.checkNotNull(id, "id");
        Utils.checkNotNull(createdAt, "createdAt");
        Utils.checkNotNull(reference, "reference");
        Utils.checkNotNull(settledAt, "settledAt");
        Utils.checkNotNull(status, "status");
        Utils.checkNotNull(amount, "amount");
        Utils.checkNotNull(balanceId, "balanceId");
        Utils.checkNotNull(invoiceId, "invoiceId");
        Utils.checkNotNull(periods, "periods");
        Utils.checkNotNull(links, "links");
        this.resource = resource;
        this.id = id;
        this.createdAt = createdAt;
        this.reference = reference;
        this.settledAt = settledAt;
        this.status = status;
        this.amount = amount;
        this.balanceId = balanceId;
        this.invoiceId = invoiceId;
        this.periods = periods;
        this.links = links;
    }
    
    public GetOpenSettlementResponseBody(
            String resource,
            OpenSettlementId id,
            Status status,
            Amount amount,
            String balanceId,
            SettlementConvenienceLinks links) {
        this(resource, id, Optional.empty(),
            JsonNullable.undefined(), JsonNullable.undefined(), status,
            amount, balanceId, JsonNullable.undefined(),
            Optional.empty(), links);
    }

    /**
     * Indicates the response contains a settlement object. Will always contain the string `settlement` for
     * this
     * endpoint.
     */
    @JsonIgnore
    public String resource() {
        return resource;
    }

    @JsonIgnore
    public OpenSettlementId id() {
        return id;
    }

    /**
     * The entity's date and time of creation, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601)
     * format.
     */
    @JsonIgnore
    public Optional<String> createdAt() {
        return createdAt;
    }

    /**
     * The settlement's bank reference, as found in your Mollie account and on your bank statement.
     */
    @JsonIgnore
    public JsonNullable<String> reference() {
        return reference;
    }

    /**
     * The date on which the settlement was settled, in ISO 8601 format.
     * 
     * <p>For an [open settlement](get-open-settlement) or for the [next settlement](get-next-settlement), no
     * settlement
     * date is available.
     */
    @JsonIgnore
    public JsonNullable<String> settledAt() {
        return settledAt;
    }

    @JsonIgnore
    public Status status() {
        return status;
    }

    @JsonIgnore
    public Amount amount() {
        return amount;
    }

    /**
     * The balance token that the settlement was settled to.
     */
    @JsonIgnore
    public String balanceId() {
        return balanceId;
    }

    /**
     * The ID of the oldest invoice created for all the periods, if the invoice has been created yet.
     */
    @JsonIgnore
    public JsonNullable<String> invoiceId() {
        return invoiceId;
    }

    /**
     * For bookkeeping purposes, the settlement includes an overview of transactions included in the
     * settlement. These
     * transactions are grouped into 'period' objects — one for each calendar month.
     * 
     * <p>For example, if a settlement includes funds from 15 April until 4 May, it will include two period
     * objects. One for
     * all transactions processed between 15 April and 30 April, and one for all transactions between 1 May
     * and 4 May.
     * 
     * <p>Period objects are grouped by year, and then by month. So in the above example, the full `periods`
     * collection will
     * look as follows: `{"2024": {"04": {...}, "05": {...&rbrace;&rbrace;}`. The year and month in this documentation
     * are referred as `&lt;year&gt;` and `&lt;month&gt;`.
     * 
     * <p>The example response should give a good idea of what this looks like in practise.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<Map<String, Map<String, Periods>>> periods() {
        return (Optional<Map<String, Map<String, Periods>>>) periods;
    }

    /**
     * An object with several relevant URLs. Every URL object will contain an `href` and a `type` field.
     * 
     * <p>This endpoint always points to your organization's current open or next settlement rather than one
     * specific
     * settlement, so it doesn't return links to that settlement's payments, captures, refunds,
     * chargebacks, or invoice.
     */
    @JsonIgnore
    public SettlementConvenienceLinks links() {
        return links;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * Indicates the response contains a settlement object. Will always contain the string `settlement` for
     * this
     * endpoint.
     */
    public GetOpenSettlementResponseBody withResource(String resource) {
        Utils.checkNotNull(resource, "resource");
        this.resource = resource;
        return this;
    }

    public GetOpenSettlementResponseBody withId(OpenSettlementId id) {
        Utils.checkNotNull(id, "id");
        this.id = id;
        return this;
    }

    /**
     * The entity's date and time of creation, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601)
     * format.
     */
    public GetOpenSettlementResponseBody withCreatedAt(String createdAt) {
        Utils.checkNotNull(createdAt, "createdAt");
        this.createdAt = Optional.ofNullable(createdAt);
        return this;
    }


    /**
     * The entity's date and time of creation, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601)
     * format.
     */
    public GetOpenSettlementResponseBody withCreatedAt(Optional<String> createdAt) {
        Utils.checkNotNull(createdAt, "createdAt");
        this.createdAt = createdAt;
        return this;
    }

    /**
     * The settlement's bank reference, as found in your Mollie account and on your bank statement.
     */
    public GetOpenSettlementResponseBody withReference(String reference) {
        Utils.checkNotNull(reference, "reference");
        this.reference = JsonNullable.of(reference);
        return this;
    }

    /**
     * The settlement's bank reference, as found in your Mollie account and on your bank statement.
     */
    public GetOpenSettlementResponseBody withReference(JsonNullable<String> reference) {
        Utils.checkNotNull(reference, "reference");
        this.reference = reference;
        return this;
    }

    /**
     * The date on which the settlement was settled, in ISO 8601 format.
     * 
     * <p>For an [open settlement](get-open-settlement) or for the [next settlement](get-next-settlement), no
     * settlement
     * date is available.
     */
    public GetOpenSettlementResponseBody withSettledAt(String settledAt) {
        Utils.checkNotNull(settledAt, "settledAt");
        this.settledAt = JsonNullable.of(settledAt);
        return this;
    }

    /**
     * The date on which the settlement was settled, in ISO 8601 format.
     * 
     * <p>For an [open settlement](get-open-settlement) or for the [next settlement](get-next-settlement), no
     * settlement
     * date is available.
     */
    public GetOpenSettlementResponseBody withSettledAt(JsonNullable<String> settledAt) {
        Utils.checkNotNull(settledAt, "settledAt");
        this.settledAt = settledAt;
        return this;
    }

    public GetOpenSettlementResponseBody withStatus(Status status) {
        Utils.checkNotNull(status, "status");
        this.status = status;
        return this;
    }

    public GetOpenSettlementResponseBody withAmount(Amount amount) {
        Utils.checkNotNull(amount, "amount");
        this.amount = amount;
        return this;
    }

    /**
     * The balance token that the settlement was settled to.
     */
    public GetOpenSettlementResponseBody withBalanceId(String balanceId) {
        Utils.checkNotNull(balanceId, "balanceId");
        this.balanceId = balanceId;
        return this;
    }

    /**
     * The ID of the oldest invoice created for all the periods, if the invoice has been created yet.
     */
    public GetOpenSettlementResponseBody withInvoiceId(String invoiceId) {
        Utils.checkNotNull(invoiceId, "invoiceId");
        this.invoiceId = JsonNullable.of(invoiceId);
        return this;
    }

    /**
     * The ID of the oldest invoice created for all the periods, if the invoice has been created yet.
     */
    public GetOpenSettlementResponseBody withInvoiceId(JsonNullable<String> invoiceId) {
        Utils.checkNotNull(invoiceId, "invoiceId");
        this.invoiceId = invoiceId;
        return this;
    }

    /**
     * For bookkeeping purposes, the settlement includes an overview of transactions included in the
     * settlement. These
     * transactions are grouped into 'period' objects — one for each calendar month.
     * 
     * <p>For example, if a settlement includes funds from 15 April until 4 May, it will include two period
     * objects. One for
     * all transactions processed between 15 April and 30 April, and one for all transactions between 1 May
     * and 4 May.
     * 
     * <p>Period objects are grouped by year, and then by month. So in the above example, the full `periods`
     * collection will
     * look as follows: `{"2024": {"04": {...}, "05": {...&rbrace;&rbrace;}`. The year and month in this documentation
     * are referred as `&lt;year&gt;` and `&lt;month&gt;`.
     * 
     * <p>The example response should give a good idea of what this looks like in practise.
     */
    public GetOpenSettlementResponseBody withPeriods(Map<String, Map<String, Periods>> periods) {
        Utils.checkNotNull(periods, "periods");
        this.periods = Optional.ofNullable(periods);
        return this;
    }


    /**
     * For bookkeeping purposes, the settlement includes an overview of transactions included in the
     * settlement. These
     * transactions are grouped into 'period' objects — one for each calendar month.
     * 
     * <p>For example, if a settlement includes funds from 15 April until 4 May, it will include two period
     * objects. One for
     * all transactions processed between 15 April and 30 April, and one for all transactions between 1 May
     * and 4 May.
     * 
     * <p>Period objects are grouped by year, and then by month. So in the above example, the full `periods`
     * collection will
     * look as follows: `{"2024": {"04": {...}, "05": {...&rbrace;&rbrace;}`. The year and month in this documentation
     * are referred as `&lt;year&gt;` and `&lt;month&gt;`.
     * 
     * <p>The example response should give a good idea of what this looks like in practise.
     */
    public GetOpenSettlementResponseBody withPeriods(Optional<? extends Map<String, Map<String, Periods>>> periods) {
        Utils.checkNotNull(periods, "periods");
        this.periods = periods;
        return this;
    }

    /**
     * An object with several relevant URLs. Every URL object will contain an `href` and a `type` field.
     * 
     * <p>This endpoint always points to your organization's current open or next settlement rather than one
     * specific
     * settlement, so it doesn't return links to that settlement's payments, captures, refunds,
     * chargebacks, or invoice.
     */
    public GetOpenSettlementResponseBody withLinks(SettlementConvenienceLinks links) {
        Utils.checkNotNull(links, "links");
        this.links = links;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        GetOpenSettlementResponseBody other = (GetOpenSettlementResponseBody) o;
        return 
            Utils.enhancedDeepEquals(this.resource, other.resource) &&
            Utils.enhancedDeepEquals(this.id, other.id) &&
            Utils.enhancedDeepEquals(this.createdAt, other.createdAt) &&
            Utils.enhancedDeepEquals(this.reference, other.reference) &&
            Utils.enhancedDeepEquals(this.settledAt, other.settledAt) &&
            Utils.enhancedDeepEquals(this.status, other.status) &&
            Utils.enhancedDeepEquals(this.amount, other.amount) &&
            Utils.enhancedDeepEquals(this.balanceId, other.balanceId) &&
            Utils.enhancedDeepEquals(this.invoiceId, other.invoiceId) &&
            Utils.enhancedDeepEquals(this.periods, other.periods) &&
            Utils.enhancedDeepEquals(this.links, other.links);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            resource, id, createdAt,
            reference, settledAt, status,
            amount, balanceId, invoiceId,
            periods, links);
    }
    
    @Override
    public String toString() {
        return Utils.toString(GetOpenSettlementResponseBody.class,
                "resource", resource,
                "id", id,
                "createdAt", createdAt,
                "reference", reference,
                "settledAt", settledAt,
                "status", status,
                "amount", amount,
                "balanceId", balanceId,
                "invoiceId", invoiceId,
                "periods", periods,
                "links", links);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String resource;

        private OpenSettlementId id;

        private Optional<String> createdAt = Optional.empty();

        private JsonNullable<String> reference = JsonNullable.undefined();

        private JsonNullable<String> settledAt = JsonNullable.undefined();

        private Status status;

        private Amount amount;

        private String balanceId;

        private JsonNullable<String> invoiceId = JsonNullable.undefined();

        private Optional<? extends Map<String, Map<String, Periods>>> periods = Optional.empty();

        private SettlementConvenienceLinks links;

        private Builder() {
          // force use of static builder() method
        }


        /**
         * Indicates the response contains a settlement object. Will always contain the string `settlement` for
         * this
         * endpoint.
         */
        public Builder resource(String resource) {
            Utils.checkNotNull(resource, "resource");
            this.resource = resource;
            return this;
        }


        public Builder id(OpenSettlementId id) {
            Utils.checkNotNull(id, "id");
            this.id = id;
            return this;
        }


        /**
         * The entity's date and time of creation, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601)
         * format.
         */
        public Builder createdAt(String createdAt) {
            Utils.checkNotNull(createdAt, "createdAt");
            this.createdAt = Optional.ofNullable(createdAt);
            return this;
        }

        /**
         * The entity's date and time of creation, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601)
         * format.
         */
        public Builder createdAt(Optional<String> createdAt) {
            Utils.checkNotNull(createdAt, "createdAt");
            this.createdAt = createdAt;
            return this;
        }


        /**
         * The settlement's bank reference, as found in your Mollie account and on your bank statement.
         */
        public Builder reference(String reference) {
            Utils.checkNotNull(reference, "reference");
            this.reference = JsonNullable.of(reference);
            return this;
        }

        /**
         * The settlement's bank reference, as found in your Mollie account and on your bank statement.
         */
        public Builder reference(JsonNullable<String> reference) {
            Utils.checkNotNull(reference, "reference");
            this.reference = reference;
            return this;
        }


        /**
         * The date on which the settlement was settled, in ISO 8601 format.
         * 
         * <p>For an [open settlement](get-open-settlement) or for the [next settlement](get-next-settlement), no
         * settlement
         * date is available.
         */
        public Builder settledAt(String settledAt) {
            Utils.checkNotNull(settledAt, "settledAt");
            this.settledAt = JsonNullable.of(settledAt);
            return this;
        }

        /**
         * The date on which the settlement was settled, in ISO 8601 format.
         * 
         * <p>For an [open settlement](get-open-settlement) or for the [next settlement](get-next-settlement), no
         * settlement
         * date is available.
         */
        public Builder settledAt(JsonNullable<String> settledAt) {
            Utils.checkNotNull(settledAt, "settledAt");
            this.settledAt = settledAt;
            return this;
        }


        public Builder status(Status status) {
            Utils.checkNotNull(status, "status");
            this.status = status;
            return this;
        }


        public Builder amount(Amount amount) {
            Utils.checkNotNull(amount, "amount");
            this.amount = amount;
            return this;
        }


        /**
         * The balance token that the settlement was settled to.
         */
        public Builder balanceId(String balanceId) {
            Utils.checkNotNull(balanceId, "balanceId");
            this.balanceId = balanceId;
            return this;
        }


        /**
         * The ID of the oldest invoice created for all the periods, if the invoice has been created yet.
         */
        public Builder invoiceId(String invoiceId) {
            Utils.checkNotNull(invoiceId, "invoiceId");
            this.invoiceId = JsonNullable.of(invoiceId);
            return this;
        }

        /**
         * The ID of the oldest invoice created for all the periods, if the invoice has been created yet.
         */
        public Builder invoiceId(JsonNullable<String> invoiceId) {
            Utils.checkNotNull(invoiceId, "invoiceId");
            this.invoiceId = invoiceId;
            return this;
        }


        /**
         * For bookkeeping purposes, the settlement includes an overview of transactions included in the
         * settlement. These
         * transactions are grouped into 'period' objects — one for each calendar month.
         * 
         * <p>For example, if a settlement includes funds from 15 April until 4 May, it will include two period
         * objects. One for
         * all transactions processed between 15 April and 30 April, and one for all transactions between 1 May
         * and 4 May.
         * 
         * <p>Period objects are grouped by year, and then by month. So in the above example, the full `periods`
         * collection will
         * look as follows: `{"2024": {"04": {...}, "05": {...&rbrace;&rbrace;}`. The year and month in this documentation
         * are referred as `&lt;year&gt;` and `&lt;month&gt;`.
         * 
         * <p>The example response should give a good idea of what this looks like in practise.
         */
        public Builder periods(Map<String, Map<String, Periods>> periods) {
            Utils.checkNotNull(periods, "periods");
            this.periods = Optional.ofNullable(periods);
            return this;
        }

        /**
         * For bookkeeping purposes, the settlement includes an overview of transactions included in the
         * settlement. These
         * transactions are grouped into 'period' objects — one for each calendar month.
         * 
         * <p>For example, if a settlement includes funds from 15 April until 4 May, it will include two period
         * objects. One for
         * all transactions processed between 15 April and 30 April, and one for all transactions between 1 May
         * and 4 May.
         * 
         * <p>Period objects are grouped by year, and then by month. So in the above example, the full `periods`
         * collection will
         * look as follows: `{"2024": {"04": {...}, "05": {...&rbrace;&rbrace;}`. The year and month in this documentation
         * are referred as `&lt;year&gt;` and `&lt;month&gt;`.
         * 
         * <p>The example response should give a good idea of what this looks like in practise.
         */
        public Builder periods(Optional<? extends Map<String, Map<String, Periods>>> periods) {
            Utils.checkNotNull(periods, "periods");
            this.periods = periods;
            return this;
        }


        /**
         * An object with several relevant URLs. Every URL object will contain an `href` and a `type` field.
         * 
         * <p>This endpoint always points to your organization's current open or next settlement rather than one
         * specific
         * settlement, so it doesn't return links to that settlement's payments, captures, refunds,
         * chargebacks, or invoice.
         */
        public Builder links(SettlementConvenienceLinks links) {
            Utils.checkNotNull(links, "links");
            this.links = links;
            return this;
        }

        public GetOpenSettlementResponseBody build() {

            return new GetOpenSettlementResponseBody(
                resource, id, createdAt,
                reference, settledAt, status,
                amount, balanceId, invoiceId,
                periods, links);
        }

    }
}
