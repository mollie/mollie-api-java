/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie.models.operations;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.mollie.mollie.utils.Utils;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.Objects;
import java.util.Optional;

/**
 * GetNextSettlementRate
 * 
 * <p>The service rates, further divided into `fixed` and `percentage` costs.
 */
public class GetNextSettlementRate {

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("fixed")
    private Optional<? extends GetNextSettlementFixed> fixed;

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("percentage")
    private Optional<? extends GetNextSettlementPercentage> percentage;

    @JsonCreator
    public GetNextSettlementRate(
            @JsonProperty("fixed") Optional<? extends GetNextSettlementFixed> fixed,
            @JsonProperty("percentage") Optional<? extends GetNextSettlementPercentage> percentage) {
        Utils.checkNotNull(fixed, "fixed");
        Utils.checkNotNull(percentage, "percentage");
        this.fixed = fixed;
        this.percentage = percentage;
    }
    
    public GetNextSettlementRate() {
        this(Optional.empty(), Optional.empty());
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<GetNextSettlementFixed> fixed() {
        return (Optional<GetNextSettlementFixed>) fixed;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<GetNextSettlementPercentage> percentage() {
        return (Optional<GetNextSettlementPercentage>) percentage;
    }

    public final static Builder builder() {
        return new Builder();
    }    

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    public GetNextSettlementRate withFixed(GetNextSettlementFixed fixed) {
        Utils.checkNotNull(fixed, "fixed");
        this.fixed = Optional.ofNullable(fixed);
        return this;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    public GetNextSettlementRate withFixed(Optional<? extends GetNextSettlementFixed> fixed) {
        Utils.checkNotNull(fixed, "fixed");
        this.fixed = fixed;
        return this;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    public GetNextSettlementRate withPercentage(GetNextSettlementPercentage percentage) {
        Utils.checkNotNull(percentage, "percentage");
        this.percentage = Optional.ofNullable(percentage);
        return this;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    public GetNextSettlementRate withPercentage(Optional<? extends GetNextSettlementPercentage> percentage) {
        Utils.checkNotNull(percentage, "percentage");
        this.percentage = percentage;
        return this;
    }

    
    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        GetNextSettlementRate other = (GetNextSettlementRate) o;
        return 
            Objects.deepEquals(this.fixed, other.fixed) &&
            Objects.deepEquals(this.percentage, other.percentage);
    }
    
    @Override
    public int hashCode() {
        return Objects.hash(
            fixed,
            percentage);
    }
    
    @Override
    public String toString() {
        return Utils.toString(GetNextSettlementRate.class,
                "fixed", fixed,
                "percentage", percentage);
    }
    
    public final static class Builder {
 
        private Optional<? extends GetNextSettlementFixed> fixed = Optional.empty();
 
        private Optional<? extends GetNextSettlementPercentage> percentage = Optional.empty();
        
        private Builder() {
          // force use of static builder() method
        }

        /**
         * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
         */
        public Builder fixed(GetNextSettlementFixed fixed) {
            Utils.checkNotNull(fixed, "fixed");
            this.fixed = Optional.ofNullable(fixed);
            return this;
        }

        /**
         * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
         */
        public Builder fixed(Optional<? extends GetNextSettlementFixed> fixed) {
            Utils.checkNotNull(fixed, "fixed");
            this.fixed = fixed;
            return this;
        }

        /**
         * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
         */
        public Builder percentage(GetNextSettlementPercentage percentage) {
            Utils.checkNotNull(percentage, "percentage");
            this.percentage = Optional.ofNullable(percentage);
            return this;
        }

        /**
         * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
         */
        public Builder percentage(Optional<? extends GetNextSettlementPercentage> percentage) {
            Utils.checkNotNull(percentage, "percentage");
            this.percentage = percentage;
            return this;
        }
        
        public GetNextSettlementRate build() {
            return new GetNextSettlementRate(
                fixed,
                percentage);
        }
    }
}
