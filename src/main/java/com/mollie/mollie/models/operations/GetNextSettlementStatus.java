/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 2b79fd91fe58
 */
package com.mollie.mollie.models.operations;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * GetNextSettlementStatus
 * 
 * <p>The status of the settlement.
 */
public class GetNextSettlementStatus {

    public static final GetNextSettlementStatus OPEN = new GetNextSettlementStatus("open");
    public static final GetNextSettlementStatus PENDING = new GetNextSettlementStatus("pending");
    public static final GetNextSettlementStatus PROCESSING_AT_BANK = new GetNextSettlementStatus("processing-at-bank");
    public static final GetNextSettlementStatus PAIDOUT = new GetNextSettlementStatus("paidout");
    public static final GetNextSettlementStatus FAILED = new GetNextSettlementStatus("failed");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, GetNextSettlementStatus> values = createValuesMap();
    private static final Map<String, GetNextSettlementStatusEnum> enums = createEnumsMap();

    private final String value;

    private GetNextSettlementStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a GetNextSettlementStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as GetNextSettlementStatus
     */ 
    @JsonCreator
    public static GetNextSettlementStatus of(String value) {
        synchronized (GetNextSettlementStatus.class) {
            return values.computeIfAbsent(value, v -> new GetNextSettlementStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<GetNextSettlementStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        GetNextSettlementStatus other = (GetNextSettlementStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "GetNextSettlementStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static GetNextSettlementStatus[] values() {
        synchronized (GetNextSettlementStatus.class) {
            return values.values().toArray(new GetNextSettlementStatus[] {});
        }
    }

    private static final Map<String, GetNextSettlementStatus> createValuesMap() {
        Map<String, GetNextSettlementStatus> map = new LinkedHashMap<>();
        map.put("open", OPEN);
        map.put("pending", PENDING);
        map.put("processing-at-bank", PROCESSING_AT_BANK);
        map.put("paidout", PAIDOUT);
        map.put("failed", FAILED);
        return map;
    }

    private static final Map<String, GetNextSettlementStatusEnum> createEnumsMap() {
        Map<String, GetNextSettlementStatusEnum> map = new HashMap<>();
        map.put("open", GetNextSettlementStatusEnum.OPEN);
        map.put("pending", GetNextSettlementStatusEnum.PENDING);
        map.put("processing-at-bank", GetNextSettlementStatusEnum.PROCESSING_AT_BANK);
        map.put("paidout", GetNextSettlementStatusEnum.PAIDOUT);
        map.put("failed", GetNextSettlementStatusEnum.FAILED);
        return map;
    }
    
    
    public enum GetNextSettlementStatusEnum {

        OPEN("open"),
        PENDING("pending"),
        PROCESSING_AT_BANK("processing-at-bank"),
        PAIDOUT("paidout"),
        FAILED("failed"),;

        private final String value;

        private GetNextSettlementStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

