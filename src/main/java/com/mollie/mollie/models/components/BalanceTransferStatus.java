/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * BalanceTransferStatus
 * 
 * <p>The status of the transfer.
 */
public class BalanceTransferStatus {

    public static final BalanceTransferStatus CREATED = new BalanceTransferStatus("created");
    public static final BalanceTransferStatus FAILED = new BalanceTransferStatus("failed");
    public static final BalanceTransferStatus SUCCEEDED = new BalanceTransferStatus("succeeded");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, BalanceTransferStatus> values = createValuesMap();
    private static final Map<String, BalanceTransferStatusEnum> enums = createEnumsMap();

    private final String value;

    private BalanceTransferStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a BalanceTransferStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as BalanceTransferStatus
     */ 
    @JsonCreator
    public static BalanceTransferStatus of(String value) {
        synchronized (BalanceTransferStatus.class) {
            return values.computeIfAbsent(value, v -> new BalanceTransferStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<BalanceTransferStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        BalanceTransferStatus other = (BalanceTransferStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "BalanceTransferStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static BalanceTransferStatus[] values() {
        synchronized (BalanceTransferStatus.class) {
            return values.values().toArray(new BalanceTransferStatus[] {});
        }
    }

    private static final Map<String, BalanceTransferStatus> createValuesMap() {
        Map<String, BalanceTransferStatus> map = new LinkedHashMap<>();
        map.put("created", CREATED);
        map.put("failed", FAILED);
        map.put("succeeded", SUCCEEDED);
        return map;
    }

    private static final Map<String, BalanceTransferStatusEnum> createEnumsMap() {
        Map<String, BalanceTransferStatusEnum> map = new HashMap<>();
        map.put("created", BalanceTransferStatusEnum.CREATED);
        map.put("failed", BalanceTransferStatusEnum.FAILED);
        map.put("succeeded", BalanceTransferStatusEnum.SUCCEEDED);
        return map;
    }
    
    
    public enum BalanceTransferStatusEnum {

        CREATED("created"),
        FAILED("failed"),
        SUCCEEDED("succeeded"),;

        private final String value;

        private BalanceTransferStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

