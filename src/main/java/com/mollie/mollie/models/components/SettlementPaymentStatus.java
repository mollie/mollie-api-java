/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: be5fbf19350d
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * SettlementPaymentStatus
 * 
 * <p>The payment's status. Settlement payments always have a status of `paid`.
 */
public class SettlementPaymentStatus {

    public static final SettlementPaymentStatus PAID = new SettlementPaymentStatus("paid");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, SettlementPaymentStatus> values = createValuesMap();
    private static final Map<String, SettlementPaymentStatusEnum> enums = createEnumsMap();

    private final String value;

    private SettlementPaymentStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a SettlementPaymentStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as SettlementPaymentStatus
     */ 
    @JsonCreator
    public static SettlementPaymentStatus of(String value) {
        synchronized (SettlementPaymentStatus.class) {
            return values.computeIfAbsent(value, v -> new SettlementPaymentStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<SettlementPaymentStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        SettlementPaymentStatus other = (SettlementPaymentStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "SettlementPaymentStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static SettlementPaymentStatus[] values() {
        synchronized (SettlementPaymentStatus.class) {
            return values.values().toArray(new SettlementPaymentStatus[] {});
        }
    }

    private static final Map<String, SettlementPaymentStatus> createValuesMap() {
        Map<String, SettlementPaymentStatus> map = new LinkedHashMap<>();
        map.put("paid", PAID);
        return map;
    }

    private static final Map<String, SettlementPaymentStatusEnum> createEnumsMap() {
        Map<String, SettlementPaymentStatusEnum> map = new HashMap<>();
        map.put("paid", SettlementPaymentStatusEnum.PAID);
        return map;
    }
    
    
    public enum SettlementPaymentStatusEnum {

        PAID("paid"),;

        private final String value;

        private SettlementPaymentStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

