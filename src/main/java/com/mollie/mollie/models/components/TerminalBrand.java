/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * TerminalBrand
 * 
 * <p>The brand of the terminal.
 */
public class TerminalBrand {

    public static final TerminalBrand PAX = new TerminalBrand("PAX");
    public static final TerminalBrand TAP = new TerminalBrand("Tap");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, TerminalBrand> values = createValuesMap();
    private static final Map<String, TerminalBrandEnum> enums = createEnumsMap();

    private final String value;

    private TerminalBrand(String value) {
        this.value = value;
    }

    /**
     * Returns a TerminalBrand with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as TerminalBrand
     */ 
    @JsonCreator
    public static TerminalBrand of(String value) {
        synchronized (TerminalBrand.class) {
            return values.computeIfAbsent(value, v -> new TerminalBrand(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<TerminalBrandEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        TerminalBrand other = (TerminalBrand) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "TerminalBrand [value=" + value + "]";
    }

    // return an array just like an enum
    public static TerminalBrand[] values() {
        synchronized (TerminalBrand.class) {
            return values.values().toArray(new TerminalBrand[] {});
        }
    }

    private static final Map<String, TerminalBrand> createValuesMap() {
        Map<String, TerminalBrand> map = new LinkedHashMap<>();
        map.put("PAX", PAX);
        map.put("Tap", TAP);
        return map;
    }

    private static final Map<String, TerminalBrandEnum> createEnumsMap() {
        Map<String, TerminalBrandEnum> map = new HashMap<>();
        map.put("PAX", TerminalBrandEnum.PAX);
        map.put("Tap", TerminalBrandEnum.TAP);
        return map;
    }
    
    
    public enum TerminalBrandEnum {

        PAX("PAX"),
        TAP("Tap"),;

        private final String value;

        private TerminalBrandEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

