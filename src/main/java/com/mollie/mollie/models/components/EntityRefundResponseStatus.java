/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class EntityRefundResponseStatus {

    public static final EntityRefundResponseStatus QUEUED = new EntityRefundResponseStatus("queued");
    public static final EntityRefundResponseStatus PENDING = new EntityRefundResponseStatus("pending");
    public static final EntityRefundResponseStatus PROCESSING = new EntityRefundResponseStatus("processing");
    public static final EntityRefundResponseStatus REFUNDED = new EntityRefundResponseStatus("refunded");
    public static final EntityRefundResponseStatus FAILED = new EntityRefundResponseStatus("failed");
    public static final EntityRefundResponseStatus CANCELED = new EntityRefundResponseStatus("canceled");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, EntityRefundResponseStatus> values = createValuesMap();
    private static final Map<String, EntityRefundResponseStatusEnum> enums = createEnumsMap();

    private final String value;

    private EntityRefundResponseStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a EntityRefundResponseStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as EntityRefundResponseStatus
     */ 
    @JsonCreator
    public static EntityRefundResponseStatus of(String value) {
        synchronized (EntityRefundResponseStatus.class) {
            return values.computeIfAbsent(value, v -> new EntityRefundResponseStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<EntityRefundResponseStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        EntityRefundResponseStatus other = (EntityRefundResponseStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "EntityRefundResponseStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static EntityRefundResponseStatus[] values() {
        synchronized (EntityRefundResponseStatus.class) {
            return values.values().toArray(new EntityRefundResponseStatus[] {});
        }
    }

    private static final Map<String, EntityRefundResponseStatus> createValuesMap() {
        Map<String, EntityRefundResponseStatus> map = new LinkedHashMap<>();
        map.put("queued", QUEUED);
        map.put("pending", PENDING);
        map.put("processing", PROCESSING);
        map.put("refunded", REFUNDED);
        map.put("failed", FAILED);
        map.put("canceled", CANCELED);
        return map;
    }

    private static final Map<String, EntityRefundResponseStatusEnum> createEnumsMap() {
        Map<String, EntityRefundResponseStatusEnum> map = new HashMap<>();
        map.put("queued", EntityRefundResponseStatusEnum.QUEUED);
        map.put("pending", EntityRefundResponseStatusEnum.PENDING);
        map.put("processing", EntityRefundResponseStatusEnum.PROCESSING);
        map.put("refunded", EntityRefundResponseStatusEnum.REFUNDED);
        map.put("failed", EntityRefundResponseStatusEnum.FAILED);
        map.put("canceled", EntityRefundResponseStatusEnum.CANCELED);
        return map;
    }
    
    
    public enum EntityRefundResponseStatusEnum {

        QUEUED("queued"),
        PENDING("pending"),
        PROCESSING("processing"),
        REFUNDED("refunded"),
        FAILED("failed"),
        CANCELED("canceled"),;

        private final String value;

        private EntityRefundResponseStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

