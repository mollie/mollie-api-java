/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 78154de0983a
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.mollie.mollie.utils.Utils;
import java.lang.Boolean;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import org.openapitools.jackson.nullable.JsonNullable;


public class TransferRequest {
    /**
     * The IBAN of the debtor's (sender) Mollie account from which to initiate the transfer.
     */
    @JsonProperty("debtorIban")
    private String debtorIban;

    /**
     * A party involved in the transfer, representing either the debtor (sender) or creditor (recipient).
     * Contains the party's name and account details.
     */
    @JsonProperty("creditor")
    private TransferParty creditor;

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    @JsonProperty("amount")
    private Amount amount;

    /**
     * A short description of the transfer. This will appear on the bank statement of both the debtor and
     * creditor.
     * 
     * <p>It must begin with an alphanumeric character, followed by any combination of letters, numbers,
     * spaces or special characters `/ -? : ( ). , ' + _`.
     * 
     * <p>Constraints:
     * - Cannot be solely a hyphen (-) or colon (:)
     * - Cannot contain consecutive hyphens (--)
     * - Cannot contain consecutive colons (::)
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("description")
    private JsonNullable<String> description;

    /**
     * The scheme, as requested by the client.
     */
    @JsonProperty("transferScheme")
    private TransferScheme transferScheme;

    /**
     * Provide any data you like, for example a string or a JSON object. We will save the data alongside
     * the entity. Whenever
     * you fetch the entity with our API, we will also include the metadata. You can use up to
     * approximately 1kB.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("metadata")
    private JsonNullable<? extends Metadata> metadata;

    /**
     * Whether to create the entity in test mode or live mode.
     * 
     * <p>Most API credentials are specifically created for either live mode or test mode, in which case this
     * parameter must
     * not be sent. For organization-level credentials such as OAuth access tokens, you can enable test
     * mode by setting
     * `testmode` to `true`.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("testmode")
    private JsonNullable<Boolean> testmode;

    @JsonCreator
    public TransferRequest(
            @JsonProperty("debtorIban") String debtorIban,
            @JsonProperty("creditor") TransferParty creditor,
            @JsonProperty("amount") Amount amount,
            @JsonProperty("description") JsonNullable<String> description,
            @JsonProperty("transferScheme") TransferScheme transferScheme,
            @JsonProperty("metadata") JsonNullable<? extends Metadata> metadata,
            @JsonProperty("testmode") JsonNullable<Boolean> testmode) {
        Utils.checkNotNull(debtorIban, "debtorIban");
        Utils.checkNotNull(creditor, "creditor");
        Utils.checkNotNull(amount, "amount");
        Utils.checkNotNull(description, "description");
        Utils.checkNotNull(transferScheme, "transferScheme");
        Utils.checkNotNull(metadata, "metadata");
        Utils.checkNotNull(testmode, "testmode");
        this.debtorIban = debtorIban;
        this.creditor = creditor;
        this.amount = amount;
        this.description = description;
        this.transferScheme = transferScheme;
        this.metadata = metadata;
        this.testmode = testmode;
    }
    
    public TransferRequest(
            String debtorIban,
            TransferParty creditor,
            Amount amount,
            TransferScheme transferScheme) {
        this(debtorIban, creditor, amount,
            JsonNullable.undefined(), transferScheme, JsonNullable.undefined(),
            JsonNullable.undefined());
    }

    /**
     * The IBAN of the debtor's (sender) Mollie account from which to initiate the transfer.
     */
    @JsonIgnore
    public String debtorIban() {
        return debtorIban;
    }

    /**
     * A party involved in the transfer, representing either the debtor (sender) or creditor (recipient).
     * Contains the party's name and account details.
     */
    @JsonIgnore
    public TransferParty creditor() {
        return creditor;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    @JsonIgnore
    public Amount amount() {
        return amount;
    }

    /**
     * A short description of the transfer. This will appear on the bank statement of both the debtor and
     * creditor.
     * 
     * <p>It must begin with an alphanumeric character, followed by any combination of letters, numbers,
     * spaces or special characters `/ -? : ( ). , ' + _`.
     * 
     * <p>Constraints:
     * - Cannot be solely a hyphen (-) or colon (:)
     * - Cannot contain consecutive hyphens (--)
     * - Cannot contain consecutive colons (::)
     */
    @JsonIgnore
    public JsonNullable<String> description() {
        return description;
    }

    /**
     * The scheme, as requested by the client.
     */
    @JsonIgnore
    public TransferScheme transferScheme() {
        return transferScheme;
    }

    /**
     * Provide any data you like, for example a string or a JSON object. We will save the data alongside
     * the entity. Whenever
     * you fetch the entity with our API, we will also include the metadata. You can use up to
     * approximately 1kB.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public JsonNullable<Metadata> metadata() {
        return (JsonNullable<Metadata>) metadata;
    }

    /**
     * Whether to create the entity in test mode or live mode.
     * 
     * <p>Most API credentials are specifically created for either live mode or test mode, in which case this
     * parameter must
     * not be sent. For organization-level credentials such as OAuth access tokens, you can enable test
     * mode by setting
     * `testmode` to `true`.
     */
    @JsonIgnore
    public JsonNullable<Boolean> testmode() {
        return testmode;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * The IBAN of the debtor's (sender) Mollie account from which to initiate the transfer.
     */
    public TransferRequest withDebtorIban(String debtorIban) {
        Utils.checkNotNull(debtorIban, "debtorIban");
        this.debtorIban = debtorIban;
        return this;
    }

    /**
     * A party involved in the transfer, representing either the debtor (sender) or creditor (recipient).
     * Contains the party's name and account details.
     */
    public TransferRequest withCreditor(TransferParty creditor) {
        Utils.checkNotNull(creditor, "creditor");
        this.creditor = creditor;
        return this;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    public TransferRequest withAmount(Amount amount) {
        Utils.checkNotNull(amount, "amount");
        this.amount = amount;
        return this;
    }

    /**
     * A short description of the transfer. This will appear on the bank statement of both the debtor and
     * creditor.
     * 
     * <p>It must begin with an alphanumeric character, followed by any combination of letters, numbers,
     * spaces or special characters `/ -? : ( ). , ' + _`.
     * 
     * <p>Constraints:
     * - Cannot be solely a hyphen (-) or colon (:)
     * - Cannot contain consecutive hyphens (--)
     * - Cannot contain consecutive colons (::)
     */
    public TransferRequest withDescription(String description) {
        Utils.checkNotNull(description, "description");
        this.description = JsonNullable.of(description);
        return this;
    }

    /**
     * A short description of the transfer. This will appear on the bank statement of both the debtor and
     * creditor.
     * 
     * <p>It must begin with an alphanumeric character, followed by any combination of letters, numbers,
     * spaces or special characters `/ -? : ( ). , ' + _`.
     * 
     * <p>Constraints:
     * - Cannot be solely a hyphen (-) or colon (:)
     * - Cannot contain consecutive hyphens (--)
     * - Cannot contain consecutive colons (::)
     */
    public TransferRequest withDescription(JsonNullable<String> description) {
        Utils.checkNotNull(description, "description");
        this.description = description;
        return this;
    }

    /**
     * The scheme, as requested by the client.
     */
    public TransferRequest withTransferScheme(TransferScheme transferScheme) {
        Utils.checkNotNull(transferScheme, "transferScheme");
        this.transferScheme = transferScheme;
        return this;
    }

    /**
     * Provide any data you like, for example a string or a JSON object. We will save the data alongside
     * the entity. Whenever
     * you fetch the entity with our API, we will also include the metadata. You can use up to
     * approximately 1kB.
     */
    public TransferRequest withMetadata(Metadata metadata) {
        Utils.checkNotNull(metadata, "metadata");
        this.metadata = JsonNullable.of(metadata);
        return this;
    }

    /**
     * Provide any data you like, for example a string or a JSON object. We will save the data alongside
     * the entity. Whenever
     * you fetch the entity with our API, we will also include the metadata. You can use up to
     * approximately 1kB.
     */
    public TransferRequest withMetadata(JsonNullable<? extends Metadata> metadata) {
        Utils.checkNotNull(metadata, "metadata");
        this.metadata = metadata;
        return this;
    }

    /**
     * Whether to create the entity in test mode or live mode.
     * 
     * <p>Most API credentials are specifically created for either live mode or test mode, in which case this
     * parameter must
     * not be sent. For organization-level credentials such as OAuth access tokens, you can enable test
     * mode by setting
     * `testmode` to `true`.
     */
    public TransferRequest withTestmode(boolean testmode) {
        Utils.checkNotNull(testmode, "testmode");
        this.testmode = JsonNullable.of(testmode);
        return this;
    }

    /**
     * Whether to create the entity in test mode or live mode.
     * 
     * <p>Most API credentials are specifically created for either live mode or test mode, in which case this
     * parameter must
     * not be sent. For organization-level credentials such as OAuth access tokens, you can enable test
     * mode by setting
     * `testmode` to `true`.
     */
    public TransferRequest withTestmode(JsonNullable<Boolean> testmode) {
        Utils.checkNotNull(testmode, "testmode");
        this.testmode = testmode;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        TransferRequest other = (TransferRequest) o;
        return 
            Utils.enhancedDeepEquals(this.debtorIban, other.debtorIban) &&
            Utils.enhancedDeepEquals(this.creditor, other.creditor) &&
            Utils.enhancedDeepEquals(this.amount, other.amount) &&
            Utils.enhancedDeepEquals(this.description, other.description) &&
            Utils.enhancedDeepEquals(this.transferScheme, other.transferScheme) &&
            Utils.enhancedDeepEquals(this.metadata, other.metadata) &&
            Utils.enhancedDeepEquals(this.testmode, other.testmode);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            debtorIban, creditor, amount,
            description, transferScheme, metadata,
            testmode);
    }
    
    @Override
    public String toString() {
        return Utils.toString(TransferRequest.class,
                "debtorIban", debtorIban,
                "creditor", creditor,
                "amount", amount,
                "description", description,
                "transferScheme", transferScheme,
                "metadata", metadata,
                "testmode", testmode);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String debtorIban;

        private TransferParty creditor;

        private Amount amount;

        private JsonNullable<String> description = JsonNullable.undefined();

        private TransferScheme transferScheme;

        private JsonNullable<? extends Metadata> metadata = JsonNullable.undefined();

        private JsonNullable<Boolean> testmode = JsonNullable.undefined();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * The IBAN of the debtor's (sender) Mollie account from which to initiate the transfer.
         */
        public Builder debtorIban(String debtorIban) {
            Utils.checkNotNull(debtorIban, "debtorIban");
            this.debtorIban = debtorIban;
            return this;
        }


        /**
         * A party involved in the transfer, representing either the debtor (sender) or creditor (recipient).
         * Contains the party's name and account details.
         */
        public Builder creditor(TransferParty creditor) {
            Utils.checkNotNull(creditor, "creditor");
            this.creditor = creditor;
            return this;
        }


        /**
         * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
         */
        public Builder amount(Amount amount) {
            Utils.checkNotNull(amount, "amount");
            this.amount = amount;
            return this;
        }


        /**
         * A short description of the transfer. This will appear on the bank statement of both the debtor and
         * creditor.
         * 
         * <p>It must begin with an alphanumeric character, followed by any combination of letters, numbers,
         * spaces or special characters `/ -? : ( ). , ' + _`.
         * 
         * <p>Constraints:
         * - Cannot be solely a hyphen (-) or colon (:)
         * - Cannot contain consecutive hyphens (--)
         * - Cannot contain consecutive colons (::)
         */
        public Builder description(String description) {
            Utils.checkNotNull(description, "description");
            this.description = JsonNullable.of(description);
            return this;
        }

        /**
         * A short description of the transfer. This will appear on the bank statement of both the debtor and
         * creditor.
         * 
         * <p>It must begin with an alphanumeric character, followed by any combination of letters, numbers,
         * spaces or special characters `/ -? : ( ). , ' + _`.
         * 
         * <p>Constraints:
         * - Cannot be solely a hyphen (-) or colon (:)
         * - Cannot contain consecutive hyphens (--)
         * - Cannot contain consecutive colons (::)
         */
        public Builder description(JsonNullable<String> description) {
            Utils.checkNotNull(description, "description");
            this.description = description;
            return this;
        }


        /**
         * The scheme, as requested by the client.
         */
        public Builder transferScheme(TransferScheme transferScheme) {
            Utils.checkNotNull(transferScheme, "transferScheme");
            this.transferScheme = transferScheme;
            return this;
        }


        /**
         * Provide any data you like, for example a string or a JSON object. We will save the data alongside
         * the entity. Whenever
         * you fetch the entity with our API, we will also include the metadata. You can use up to
         * approximately 1kB.
         */
        public Builder metadata(Metadata metadata) {
            Utils.checkNotNull(metadata, "metadata");
            this.metadata = JsonNullable.of(metadata);
            return this;
        }

        /**
         * Provide any data you like, for example a string or a JSON object. We will save the data alongside
         * the entity. Whenever
         * you fetch the entity with our API, we will also include the metadata. You can use up to
         * approximately 1kB.
         */
        public Builder metadata(JsonNullable<? extends Metadata> metadata) {
            Utils.checkNotNull(metadata, "metadata");
            this.metadata = metadata;
            return this;
        }


        /**
         * Whether to create the entity in test mode or live mode.
         * 
         * <p>Most API credentials are specifically created for either live mode or test mode, in which case this
         * parameter must
         * not be sent. For organization-level credentials such as OAuth access tokens, you can enable test
         * mode by setting
         * `testmode` to `true`.
         */
        public Builder testmode(boolean testmode) {
            Utils.checkNotNull(testmode, "testmode");
            this.testmode = JsonNullable.of(testmode);
            return this;
        }

        /**
         * Whether to create the entity in test mode or live mode.
         * 
         * <p>Most API credentials are specifically created for either live mode or test mode, in which case this
         * parameter must
         * not be sent. For organization-level credentials such as OAuth access tokens, you can enable test
         * mode by setting
         * `testmode` to `true`.
         */
        public Builder testmode(JsonNullable<Boolean> testmode) {
            Utils.checkNotNull(testmode, "testmode");
            this.testmode = testmode;
            return this;
        }

        public TransferRequest build() {

            return new TransferRequest(
                debtorIban, creditor, amount,
                description, transferScheme, metadata,
                testmode);
        }

    }
}
