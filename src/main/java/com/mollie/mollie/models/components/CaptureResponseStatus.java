/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * CaptureResponseStatus
 * 
 * <p>The capture's status.
 */
public class CaptureResponseStatus {

    public static final CaptureResponseStatus PENDING = new CaptureResponseStatus("pending");
    public static final CaptureResponseStatus SUCCEEDED = new CaptureResponseStatus("succeeded");
    public static final CaptureResponseStatus FAILED = new CaptureResponseStatus("failed");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, CaptureResponseStatus> values = createValuesMap();
    private static final Map<String, CaptureResponseStatusEnum> enums = createEnumsMap();

    private final String value;

    private CaptureResponseStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a CaptureResponseStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as CaptureResponseStatus
     */ 
    @JsonCreator
    public static CaptureResponseStatus of(String value) {
        synchronized (CaptureResponseStatus.class) {
            return values.computeIfAbsent(value, v -> new CaptureResponseStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<CaptureResponseStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        CaptureResponseStatus other = (CaptureResponseStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "CaptureResponseStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static CaptureResponseStatus[] values() {
        synchronized (CaptureResponseStatus.class) {
            return values.values().toArray(new CaptureResponseStatus[] {});
        }
    }

    private static final Map<String, CaptureResponseStatus> createValuesMap() {
        Map<String, CaptureResponseStatus> map = new LinkedHashMap<>();
        map.put("pending", PENDING);
        map.put("succeeded", SUCCEEDED);
        map.put("failed", FAILED);
        return map;
    }

    private static final Map<String, CaptureResponseStatusEnum> createEnumsMap() {
        Map<String, CaptureResponseStatusEnum> map = new HashMap<>();
        map.put("pending", CaptureResponseStatusEnum.PENDING);
        map.put("succeeded", CaptureResponseStatusEnum.SUCCEEDED);
        map.put("failed", CaptureResponseStatusEnum.FAILED);
        return map;
    }
    
    
    public enum CaptureResponseStatusEnum {

        PENDING("pending"),
        SUCCEEDED("succeeded"),
        FAILED("failed"),;

        private final String value;

        private CaptureResponseStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

