/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 9b8ba92e6bd0
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * Scopes
 * 
 * <p>An array defining the eligible use cases for the mandate. For creditcard mandates, this field will
 * always be
 * present and can contain one or both of the following values:
 */
public class Scopes {

    public static final Scopes CUSTOMER_PRESENT = new Scopes("customer-present");
    public static final Scopes CUSTOMER_NOT_PRESENT = new Scopes("customer-not-present");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, Scopes> values = createValuesMap();
    private static final Map<String, ScopesEnum> enums = createEnumsMap();

    private final String value;

    private Scopes(String value) {
        this.value = value;
    }

    /**
     * Returns a Scopes with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as Scopes
     */ 
    @JsonCreator
    public static Scopes of(String value) {
        synchronized (Scopes.class) {
            return values.computeIfAbsent(value, v -> new Scopes(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ScopesEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        Scopes other = (Scopes) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "Scopes [value=" + value + "]";
    }

    // return an array just like an enum
    public static Scopes[] values() {
        synchronized (Scopes.class) {
            return values.values().toArray(new Scopes[] {});
        }
    }

    private static final Map<String, Scopes> createValuesMap() {
        Map<String, Scopes> map = new LinkedHashMap<>();
        map.put("customer-present", CUSTOMER_PRESENT);
        map.put("customer-not-present", CUSTOMER_NOT_PRESENT);
        return map;
    }

    private static final Map<String, ScopesEnum> createEnumsMap() {
        Map<String, ScopesEnum> map = new HashMap<>();
        map.put("customer-present", ScopesEnum.CUSTOMER_PRESENT);
        map.put("customer-not-present", ScopesEnum.CUSTOMER_NOT_PRESENT);
        return map;
    }
    
    
    public enum ScopesEnum {

        CUSTOMER_PRESENT("customer-present"),
        CUSTOMER_NOT_PRESENT("customer-not-present"),;

        private final String value;

        private ScopesEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

