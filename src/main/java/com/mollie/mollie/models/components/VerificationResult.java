/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 2d85ccad5260
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * VerificationResult
 * 
 * <p>The result of the Verification of Payee check.
 */
public class VerificationResult {

    public static final VerificationResult MATCH = new VerificationResult("match");
    public static final VerificationResult NO_MATCH = new VerificationResult("no-match");
    public static final VerificationResult CLOSE_MATCH = new VerificationResult("close-match");
    public static final VerificationResult NOT_AVAILABLE = new VerificationResult("not-available");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, VerificationResult> values = createValuesMap();
    private static final Map<String, VerificationResultEnum> enums = createEnumsMap();

    private final String value;

    private VerificationResult(String value) {
        this.value = value;
    }

    /**
     * Returns a VerificationResult with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as VerificationResult
     */ 
    @JsonCreator
    public static VerificationResult of(String value) {
        synchronized (VerificationResult.class) {
            return values.computeIfAbsent(value, v -> new VerificationResult(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<VerificationResultEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        VerificationResult other = (VerificationResult) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "VerificationResult [value=" + value + "]";
    }

    // return an array just like an enum
    public static VerificationResult[] values() {
        synchronized (VerificationResult.class) {
            return values.values().toArray(new VerificationResult[] {});
        }
    }

    private static final Map<String, VerificationResult> createValuesMap() {
        Map<String, VerificationResult> map = new LinkedHashMap<>();
        map.put("match", MATCH);
        map.put("no-match", NO_MATCH);
        map.put("close-match", CLOSE_MATCH);
        map.put("not-available", NOT_AVAILABLE);
        return map;
    }

    private static final Map<String, VerificationResultEnum> createEnumsMap() {
        Map<String, VerificationResultEnum> map = new HashMap<>();
        map.put("match", VerificationResultEnum.MATCH);
        map.put("no-match", VerificationResultEnum.NO_MATCH);
        map.put("close-match", VerificationResultEnum.CLOSE_MATCH);
        map.put("not-available", VerificationResultEnum.NOT_AVAILABLE);
        return map;
    }
    
    
    public enum VerificationResultEnum {

        MATCH("match"),
        NO_MATCH("no-match"),
        CLOSE_MATCH("close-match"),
        NOT_AVAILABLE("not-available"),;

        private final String value;

        private VerificationResultEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

