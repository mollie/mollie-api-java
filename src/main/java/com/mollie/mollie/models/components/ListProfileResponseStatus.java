/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * <p>Wrapper class for an "open" enum. "Open" enums are those that are expected
 * to evolve (particularly with the addition of enum members over time). If an
 * open enum is used then the appearance of unexpected enum values (say in a 
 * response from an updated an API) will not bring about a runtime error thus 
 * ensuring that non-updated client versions can continue to work without error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe
 * cache is maintained to ensure that). As a consequence instances created with the 
 * same value will satisfy reference equality (via {@code ==}).
 * 
 * <p>This class is intended to emulate an enum (in terms of common usage and with 
 * reference equality) but with the ability to carry unknown values. Unfortunately
 * Java does not permit the use of an instance in a switch expression but you can 
 * use the {@code asEnum()} method (after dealing with the `Optional` appropriately).
 *
 */
/**
 * ListProfileResponseStatus
 * 
 * <p>The profile status determines whether the profile is able to receive live payments.
 * 
 * <p>* `unverified`: The profile has not been verified yet and can only be used to create test payments.
 * * `verified`: The profile has been verified and can be used to create live payments and test
 * payments.
 * * `blocked`: The profile is blocked and can no longer be used or changed.
 */
@JsonDeserialize(using = ListProfileResponseStatus._Deserializer.class)
@JsonSerialize(using = ListProfileResponseStatus._Serializer.class)
public class ListProfileResponseStatus {

    public static final ListProfileResponseStatus UNVERIFIED = new ListProfileResponseStatus("unverified");
    public static final ListProfileResponseStatus VERIFIED = new ListProfileResponseStatus("verified");
    public static final ListProfileResponseStatus BLOCKED = new ListProfileResponseStatus("blocked");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListProfileResponseStatus> values = createValuesMap();
    private static final Map<String, ListProfileResponseStatusEnum> enums = createEnumsMap();

    private final String value;

    private ListProfileResponseStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a ListProfileResponseStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListProfileResponseStatus
     */ 
    public static ListProfileResponseStatus of(String value) {
        synchronized (ListProfileResponseStatus.class) {
            return values.computeIfAbsent(value, v -> new ListProfileResponseStatus(v));
        }
    }

    public String value() {
        return value;
    }

    public Optional<ListProfileResponseStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListProfileResponseStatus other = (ListProfileResponseStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListProfileResponseStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListProfileResponseStatus[] values() {
        synchronized (ListProfileResponseStatus.class) {
            return values.values().toArray(new ListProfileResponseStatus[] {});
        }
    }

    private static final Map<String, ListProfileResponseStatus> createValuesMap() {
        Map<String, ListProfileResponseStatus> map = new LinkedHashMap<>();
        map.put("unverified", UNVERIFIED);
        map.put("verified", VERIFIED);
        map.put("blocked", BLOCKED);
        return map;
    }

    private static final Map<String, ListProfileResponseStatusEnum> createEnumsMap() {
        Map<String, ListProfileResponseStatusEnum> map = new HashMap<>();
        map.put("unverified", ListProfileResponseStatusEnum.UNVERIFIED);
        map.put("verified", ListProfileResponseStatusEnum.VERIFIED);
        map.put("blocked", ListProfileResponseStatusEnum.BLOCKED);
        return map;
    }
    
    @SuppressWarnings("serial")
    public static final class _Serializer extends StdSerializer<ListProfileResponseStatus> {

        protected _Serializer() {
            super(ListProfileResponseStatus.class);
        }

        @Override
        public void serialize(ListProfileResponseStatus value, JsonGenerator g, SerializerProvider provider)
                throws IOException, JsonProcessingException {
            g.writeObject(value.value);
        }
    }

    @SuppressWarnings("serial")
    public static final class _Deserializer extends StdDeserializer<ListProfileResponseStatus> {

        protected _Deserializer() {
            super(ListProfileResponseStatus.class);
        }

        @Override
        public ListProfileResponseStatus deserialize(JsonParser p, DeserializationContext ctxt)
                throws IOException, JacksonException {
            String v = p.readValueAs(new TypeReference<String>() {});
            // use the factory method to ensure we get singletons
            return ListProfileResponseStatus.of(v);
        }
    }
    
    public enum ListProfileResponseStatusEnum {

        UNVERIFIED("unverified"),
        VERIFIED("verified"),
        BLOCKED("blocked"),;

        private final String value;

        private ListProfileResponseStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

