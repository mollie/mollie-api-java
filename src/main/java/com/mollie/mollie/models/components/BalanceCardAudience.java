/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class BalanceCardAudience {

    public static final BalanceCardAudience CORPORATE = new BalanceCardAudience("corporate");
    public static final BalanceCardAudience OTHER = new BalanceCardAudience("other");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, BalanceCardAudience> values = createValuesMap();
    private static final Map<String, BalanceCardAudienceEnum> enums = createEnumsMap();

    private final String value;

    private BalanceCardAudience(String value) {
        this.value = value;
    }

    /**
     * Returns a BalanceCardAudience with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as BalanceCardAudience
     */ 
    @JsonCreator
    public static BalanceCardAudience of(String value) {
        synchronized (BalanceCardAudience.class) {
            return values.computeIfAbsent(value, v -> new BalanceCardAudience(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<BalanceCardAudienceEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        BalanceCardAudience other = (BalanceCardAudience) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "BalanceCardAudience [value=" + value + "]";
    }

    // return an array just like an enum
    public static BalanceCardAudience[] values() {
        synchronized (BalanceCardAudience.class) {
            return values.values().toArray(new BalanceCardAudience[] {});
        }
    }

    private static final Map<String, BalanceCardAudience> createValuesMap() {
        Map<String, BalanceCardAudience> map = new LinkedHashMap<>();
        map.put("corporate", CORPORATE);
        map.put("other", OTHER);
        return map;
    }

    private static final Map<String, BalanceCardAudienceEnum> createEnumsMap() {
        Map<String, BalanceCardAudienceEnum> map = new HashMap<>();
        map.put("corporate", BalanceCardAudienceEnum.CORPORATE);
        map.put("other", BalanceCardAudienceEnum.OTHER);
        return map;
    }
    
    
    public enum BalanceCardAudienceEnum {

        CORPORATE("corporate"),
        OTHER("other"),;

        private final String value;

        private BalanceCardAudienceEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

