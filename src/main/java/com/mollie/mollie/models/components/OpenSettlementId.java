/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 21cd4bdb5c75
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class OpenSettlementId {

    public static final OpenSettlementId OPEN = new OpenSettlementId("open");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, OpenSettlementId> values = createValuesMap();
    private static final Map<String, OpenSettlementIdEnum> enums = createEnumsMap();

    private final String value;

    private OpenSettlementId(String value) {
        this.value = value;
    }

    /**
     * Returns a OpenSettlementId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as OpenSettlementId
     */ 
    @JsonCreator
    public static OpenSettlementId of(String value) {
        synchronized (OpenSettlementId.class) {
            return values.computeIfAbsent(value, v -> new OpenSettlementId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<OpenSettlementIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        OpenSettlementId other = (OpenSettlementId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "OpenSettlementId [value=" + value + "]";
    }

    // return an array just like an enum
    public static OpenSettlementId[] values() {
        synchronized (OpenSettlementId.class) {
            return values.values().toArray(new OpenSettlementId[] {});
        }
    }

    private static final Map<String, OpenSettlementId> createValuesMap() {
        Map<String, OpenSettlementId> map = new LinkedHashMap<>();
        map.put("open", OPEN);
        return map;
    }

    private static final Map<String, OpenSettlementIdEnum> createEnumsMap() {
        Map<String, OpenSettlementIdEnum> map = new HashMap<>();
        map.put("open", OpenSettlementIdEnum.OPEN);
        return map;
    }
    
    
    public enum OpenSettlementIdEnum {

        OPEN("open"),;

        private final String value;

        private OpenSettlementIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

