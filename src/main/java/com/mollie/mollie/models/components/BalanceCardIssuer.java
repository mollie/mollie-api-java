/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * <p>Wrapper class for an "open" enum. "Open" enums are those that are expected
 * to evolve (particularly with the addition of enum members over time). If an
 * open enum is used then the appearance of unexpected enum values (say in a 
 * response from an updated an API) will not bring about a runtime error thus 
 * ensuring that non-updated client versions can continue to work without error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe
 * cache is maintained to ensure that). As a consequence instances created with the 
 * same value will satisfy reference equality (via {@code ==}).
 * 
 * <p>This class is intended to emulate an enum (in terms of common usage and with 
 * reference equality) but with the ability to carry unknown values. Unfortunately
 * Java does not permit the use of an instance in a switch expression but you can 
 * use the {@code asEnum()} method (after dealing with the `Optional` appropriately).
 *
 */
@JsonDeserialize(using = BalanceCardIssuer._Deserializer.class)
@JsonSerialize(using = BalanceCardIssuer._Serializer.class)
public class BalanceCardIssuer {

    public static final BalanceCardIssuer AMEX = new BalanceCardIssuer("amex");
    public static final BalanceCardIssuer MAESTRO = new BalanceCardIssuer("maestro");
    public static final BalanceCardIssuer CARTE_BANCAIRE = new BalanceCardIssuer("carte-bancaire");
    public static final BalanceCardIssuer OTHER = new BalanceCardIssuer("other");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, BalanceCardIssuer> values = createValuesMap();
    private static final Map<String, BalanceCardIssuerEnum> enums = createEnumsMap();

    private final String value;

    private BalanceCardIssuer(String value) {
        this.value = value;
    }

    /**
     * Returns a BalanceCardIssuer with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as BalanceCardIssuer
     */ 
    public static BalanceCardIssuer of(String value) {
        synchronized (BalanceCardIssuer.class) {
            return values.computeIfAbsent(value, v -> new BalanceCardIssuer(v));
        }
    }

    public String value() {
        return value;
    }

    public Optional<BalanceCardIssuerEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        BalanceCardIssuer other = (BalanceCardIssuer) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "BalanceCardIssuer [value=" + value + "]";
    }

    // return an array just like an enum
    public static BalanceCardIssuer[] values() {
        synchronized (BalanceCardIssuer.class) {
            return values.values().toArray(new BalanceCardIssuer[] {});
        }
    }

    private static final Map<String, BalanceCardIssuer> createValuesMap() {
        Map<String, BalanceCardIssuer> map = new LinkedHashMap<>();
        map.put("amex", AMEX);
        map.put("maestro", MAESTRO);
        map.put("carte-bancaire", CARTE_BANCAIRE);
        map.put("other", OTHER);
        return map;
    }

    private static final Map<String, BalanceCardIssuerEnum> createEnumsMap() {
        Map<String, BalanceCardIssuerEnum> map = new HashMap<>();
        map.put("amex", BalanceCardIssuerEnum.AMEX);
        map.put("maestro", BalanceCardIssuerEnum.MAESTRO);
        map.put("carte-bancaire", BalanceCardIssuerEnum.CARTE_BANCAIRE);
        map.put("other", BalanceCardIssuerEnum.OTHER);
        return map;
    }
    
    @SuppressWarnings("serial")
    public static final class _Serializer extends StdSerializer<BalanceCardIssuer> {

        protected _Serializer() {
            super(BalanceCardIssuer.class);
        }

        @Override
        public void serialize(BalanceCardIssuer value, JsonGenerator g, SerializerProvider provider)
                throws IOException, JsonProcessingException {
            g.writeObject(value.value);
        }
    }

    @SuppressWarnings("serial")
    public static final class _Deserializer extends StdDeserializer<BalanceCardIssuer> {

        protected _Deserializer() {
            super(BalanceCardIssuer.class);
        }

        @Override
        public BalanceCardIssuer deserialize(JsonParser p, DeserializationContext ctxt)
                throws IOException, JacksonException {
            String v = p.readValueAs(new TypeReference<String>() {});
            // use the factory method to ensure we get singletons
            return BalanceCardIssuer.of(v);
        }
    }
    
    public enum BalanceCardIssuerEnum {

        AMEX("amex"),
        MAESTRO("maestro"),
        CARTE_BANCAIRE("carte-bancaire"),
        OTHER("other"),;

        private final String value;

        private BalanceCardIssuerEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

