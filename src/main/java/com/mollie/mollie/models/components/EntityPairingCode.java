/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: c19c1de90858
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.mollie.mollie.utils.Utils;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import org.openapitools.jackson.nullable.JsonNullable;


public class EntityPairingCode {
    /**
     * Indicates the response contains a terminal pairing code object. Will always contain the string
     * `terminal-pairing-code` for this endpoint.
     */
    @JsonProperty("resource")
    private String resource;

    /**
     * The unique identifier of the pairing code. Example: `termpc_R7gX5Ea9bC4DkFj3G`.
     */
    @JsonProperty("id")
    private String id;

    /**
     * Whether this entity was created in live mode or in test mode.
     */
    @JsonProperty("mode")
    private Mode mode;

    /**
     * The human-readable pairing code to be entered on the terminal. This code is multi-use and will
     * expire after the
     * time indicated in `expiresAt`.
     */
    @JsonProperty("code")
    private String code;

    /**
     * The ID of the profile the terminal is being paired with.
     */
    @JsonProperty("profileId")
    private String profileId;


    @JsonProperty("status")
    private EntityPairingCodeStatus status;

    /**
     * Additional pairing code data, present only when requested via the `include` parameter.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("details")
    private JsonNullable<? extends EntityPairingCodeDetails> details;

    /**
     * The date and time the pairing code expires, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601)
     * format. Pairing
     * codes expire 90 days after creation. After this time, the code can no longer be used to pair a
     * terminal.
     */
    @JsonProperty("expiresAt")
    private String expiresAt;

    /**
     * The date and time the pairing code was revoked, in [ISO
     * 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
     * `null` if the code has not been revoked.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("revokedAt")
    private JsonNullable<String> revokedAt;

    /**
     * The entity's date and time of creation, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601)
     * format.
     */
    @JsonProperty("createdAt")
    private String createdAt;

    /**
     * An object with several relevant URLs. Every URL object will contain an `href` and a `type` field.
     */
    @JsonProperty("_links")
    private EntityPairingCodeLinks links;

    @JsonCreator
    public EntityPairingCode(
            @JsonProperty("resource") String resource,
            @JsonProperty("id") String id,
            @JsonProperty("mode") Mode mode,
            @JsonProperty("code") String code,
            @JsonProperty("profileId") String profileId,
            @JsonProperty("status") EntityPairingCodeStatus status,
            @JsonProperty("details") JsonNullable<? extends EntityPairingCodeDetails> details,
            @JsonProperty("expiresAt") String expiresAt,
            @JsonProperty("revokedAt") JsonNullable<String> revokedAt,
            @JsonProperty("createdAt") String createdAt,
            @JsonProperty("_links") EntityPairingCodeLinks links) {
        Utils.checkNotNull(resource, "resource");
        Utils.checkNotNull(id, "id");
        Utils.checkNotNull(mode, "mode");
        Utils.checkNotNull(code, "code");
        Utils.checkNotNull(profileId, "profileId");
        Utils.checkNotNull(status, "status");
        Utils.checkNotNull(details, "details");
        Utils.checkNotNull(expiresAt, "expiresAt");
        Utils.checkNotNull(revokedAt, "revokedAt");
        Utils.checkNotNull(createdAt, "createdAt");
        Utils.checkNotNull(links, "links");
        this.resource = resource;
        this.id = id;
        this.mode = mode;
        this.code = code;
        this.profileId = profileId;
        this.status = status;
        this.details = details;
        this.expiresAt = expiresAt;
        this.revokedAt = revokedAt;
        this.createdAt = createdAt;
        this.links = links;
    }
    
    public EntityPairingCode(
            String resource,
            String id,
            Mode mode,
            String code,
            String profileId,
            EntityPairingCodeStatus status,
            String expiresAt,
            String createdAt,
            EntityPairingCodeLinks links) {
        this(resource, id, mode,
            code, profileId, status,
            JsonNullable.undefined(), expiresAt, JsonNullable.undefined(),
            createdAt, links);
    }

    /**
     * Indicates the response contains a terminal pairing code object. Will always contain the string
     * `terminal-pairing-code` for this endpoint.
     */
    @JsonIgnore
    public String resource() {
        return resource;
    }

    /**
     * The unique identifier of the pairing code. Example: `termpc_R7gX5Ea9bC4DkFj3G`.
     */
    @JsonIgnore
    public String id() {
        return id;
    }

    /**
     * Whether this entity was created in live mode or in test mode.
     */
    @JsonIgnore
    public Mode mode() {
        return mode;
    }

    /**
     * The human-readable pairing code to be entered on the terminal. This code is multi-use and will
     * expire after the
     * time indicated in `expiresAt`.
     */
    @JsonIgnore
    public String code() {
        return code;
    }

    /**
     * The ID of the profile the terminal is being paired with.
     */
    @JsonIgnore
    public String profileId() {
        return profileId;
    }

    @JsonIgnore
    public EntityPairingCodeStatus status() {
        return status;
    }

    /**
     * Additional pairing code data, present only when requested via the `include` parameter.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public JsonNullable<EntityPairingCodeDetails> details() {
        return (JsonNullable<EntityPairingCodeDetails>) details;
    }

    /**
     * The date and time the pairing code expires, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601)
     * format. Pairing
     * codes expire 90 days after creation. After this time, the code can no longer be used to pair a
     * terminal.
     */
    @JsonIgnore
    public String expiresAt() {
        return expiresAt;
    }

    /**
     * The date and time the pairing code was revoked, in [ISO
     * 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
     * `null` if the code has not been revoked.
     */
    @JsonIgnore
    public JsonNullable<String> revokedAt() {
        return revokedAt;
    }

    /**
     * The entity's date and time of creation, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601)
     * format.
     */
    @JsonIgnore
    public String createdAt() {
        return createdAt;
    }

    /**
     * An object with several relevant URLs. Every URL object will contain an `href` and a `type` field.
     */
    @JsonIgnore
    public EntityPairingCodeLinks links() {
        return links;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * Indicates the response contains a terminal pairing code object. Will always contain the string
     * `terminal-pairing-code` for this endpoint.
     */
    public EntityPairingCode withResource(String resource) {
        Utils.checkNotNull(resource, "resource");
        this.resource = resource;
        return this;
    }

    /**
     * The unique identifier of the pairing code. Example: `termpc_R7gX5Ea9bC4DkFj3G`.
     */
    public EntityPairingCode withId(String id) {
        Utils.checkNotNull(id, "id");
        this.id = id;
        return this;
    }

    /**
     * Whether this entity was created in live mode or in test mode.
     */
    public EntityPairingCode withMode(Mode mode) {
        Utils.checkNotNull(mode, "mode");
        this.mode = mode;
        return this;
    }

    /**
     * The human-readable pairing code to be entered on the terminal. This code is multi-use and will
     * expire after the
     * time indicated in `expiresAt`.
     */
    public EntityPairingCode withCode(String code) {
        Utils.checkNotNull(code, "code");
        this.code = code;
        return this;
    }

    /**
     * The ID of the profile the terminal is being paired with.
     */
    public EntityPairingCode withProfileId(String profileId) {
        Utils.checkNotNull(profileId, "profileId");
        this.profileId = profileId;
        return this;
    }

    public EntityPairingCode withStatus(EntityPairingCodeStatus status) {
        Utils.checkNotNull(status, "status");
        this.status = status;
        return this;
    }

    /**
     * Additional pairing code data, present only when requested via the `include` parameter.
     */
    public EntityPairingCode withDetails(EntityPairingCodeDetails details) {
        Utils.checkNotNull(details, "details");
        this.details = JsonNullable.of(details);
        return this;
    }

    /**
     * Additional pairing code data, present only when requested via the `include` parameter.
     */
    public EntityPairingCode withDetails(JsonNullable<? extends EntityPairingCodeDetails> details) {
        Utils.checkNotNull(details, "details");
        this.details = details;
        return this;
    }

    /**
     * The date and time the pairing code expires, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601)
     * format. Pairing
     * codes expire 90 days after creation. After this time, the code can no longer be used to pair a
     * terminal.
     */
    public EntityPairingCode withExpiresAt(String expiresAt) {
        Utils.checkNotNull(expiresAt, "expiresAt");
        this.expiresAt = expiresAt;
        return this;
    }

    /**
     * The date and time the pairing code was revoked, in [ISO
     * 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
     * `null` if the code has not been revoked.
     */
    public EntityPairingCode withRevokedAt(String revokedAt) {
        Utils.checkNotNull(revokedAt, "revokedAt");
        this.revokedAt = JsonNullable.of(revokedAt);
        return this;
    }

    /**
     * The date and time the pairing code was revoked, in [ISO
     * 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
     * `null` if the code has not been revoked.
     */
    public EntityPairingCode withRevokedAt(JsonNullable<String> revokedAt) {
        Utils.checkNotNull(revokedAt, "revokedAt");
        this.revokedAt = revokedAt;
        return this;
    }

    /**
     * The entity's date and time of creation, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601)
     * format.
     */
    public EntityPairingCode withCreatedAt(String createdAt) {
        Utils.checkNotNull(createdAt, "createdAt");
        this.createdAt = createdAt;
        return this;
    }

    /**
     * An object with several relevant URLs. Every URL object will contain an `href` and a `type` field.
     */
    public EntityPairingCode withLinks(EntityPairingCodeLinks links) {
        Utils.checkNotNull(links, "links");
        this.links = links;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        EntityPairingCode other = (EntityPairingCode) o;
        return 
            Utils.enhancedDeepEquals(this.resource, other.resource) &&
            Utils.enhancedDeepEquals(this.id, other.id) &&
            Utils.enhancedDeepEquals(this.mode, other.mode) &&
            Utils.enhancedDeepEquals(this.code, other.code) &&
            Utils.enhancedDeepEquals(this.profileId, other.profileId) &&
            Utils.enhancedDeepEquals(this.status, other.status) &&
            Utils.enhancedDeepEquals(this.details, other.details) &&
            Utils.enhancedDeepEquals(this.expiresAt, other.expiresAt) &&
            Utils.enhancedDeepEquals(this.revokedAt, other.revokedAt) &&
            Utils.enhancedDeepEquals(this.createdAt, other.createdAt) &&
            Utils.enhancedDeepEquals(this.links, other.links);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            resource, id, mode,
            code, profileId, status,
            details, expiresAt, revokedAt,
            createdAt, links);
    }
    
    @Override
    public String toString() {
        return Utils.toString(EntityPairingCode.class,
                "resource", resource,
                "id", id,
                "mode", mode,
                "code", code,
                "profileId", profileId,
                "status", status,
                "details", details,
                "expiresAt", expiresAt,
                "revokedAt", revokedAt,
                "createdAt", createdAt,
                "links", links);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String resource;

        private String id;

        private Mode mode;

        private String code;

        private String profileId;

        private EntityPairingCodeStatus status;

        private JsonNullable<? extends EntityPairingCodeDetails> details = JsonNullable.undefined();

        private String expiresAt;

        private JsonNullable<String> revokedAt = JsonNullable.undefined();

        private String createdAt;

        private EntityPairingCodeLinks links;

        private Builder() {
          // force use of static builder() method
        }


        /**
         * Indicates the response contains a terminal pairing code object. Will always contain the string
         * `terminal-pairing-code` for this endpoint.
         */
        public Builder resource(String resource) {
            Utils.checkNotNull(resource, "resource");
            this.resource = resource;
            return this;
        }


        /**
         * The unique identifier of the pairing code. Example: `termpc_R7gX5Ea9bC4DkFj3G`.
         */
        public Builder id(String id) {
            Utils.checkNotNull(id, "id");
            this.id = id;
            return this;
        }


        /**
         * Whether this entity was created in live mode or in test mode.
         */
        public Builder mode(Mode mode) {
            Utils.checkNotNull(mode, "mode");
            this.mode = mode;
            return this;
        }


        /**
         * The human-readable pairing code to be entered on the terminal. This code is multi-use and will
         * expire after the
         * time indicated in `expiresAt`.
         */
        public Builder code(String code) {
            Utils.checkNotNull(code, "code");
            this.code = code;
            return this;
        }


        /**
         * The ID of the profile the terminal is being paired with.
         */
        public Builder profileId(String profileId) {
            Utils.checkNotNull(profileId, "profileId");
            this.profileId = profileId;
            return this;
        }


        public Builder status(EntityPairingCodeStatus status) {
            Utils.checkNotNull(status, "status");
            this.status = status;
            return this;
        }


        /**
         * Additional pairing code data, present only when requested via the `include` parameter.
         */
        public Builder details(EntityPairingCodeDetails details) {
            Utils.checkNotNull(details, "details");
            this.details = JsonNullable.of(details);
            return this;
        }

        /**
         * Additional pairing code data, present only when requested via the `include` parameter.
         */
        public Builder details(JsonNullable<? extends EntityPairingCodeDetails> details) {
            Utils.checkNotNull(details, "details");
            this.details = details;
            return this;
        }


        /**
         * The date and time the pairing code expires, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601)
         * format. Pairing
         * codes expire 90 days after creation. After this time, the code can no longer be used to pair a
         * terminal.
         */
        public Builder expiresAt(String expiresAt) {
            Utils.checkNotNull(expiresAt, "expiresAt");
            this.expiresAt = expiresAt;
            return this;
        }


        /**
         * The date and time the pairing code was revoked, in [ISO
         * 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
         * `null` if the code has not been revoked.
         */
        public Builder revokedAt(String revokedAt) {
            Utils.checkNotNull(revokedAt, "revokedAt");
            this.revokedAt = JsonNullable.of(revokedAt);
            return this;
        }

        /**
         * The date and time the pairing code was revoked, in [ISO
         * 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
         * `null` if the code has not been revoked.
         */
        public Builder revokedAt(JsonNullable<String> revokedAt) {
            Utils.checkNotNull(revokedAt, "revokedAt");
            this.revokedAt = revokedAt;
            return this;
        }


        /**
         * The entity's date and time of creation, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601)
         * format.
         */
        public Builder createdAt(String createdAt) {
            Utils.checkNotNull(createdAt, "createdAt");
            this.createdAt = createdAt;
            return this;
        }


        /**
         * An object with several relevant URLs. Every URL object will contain an `href` and a `type` field.
         */
        public Builder links(EntityPairingCodeLinks links) {
            Utils.checkNotNull(links, "links");
            this.links = links;
            return this;
        }

        public EntityPairingCode build() {

            return new EntityPairingCode(
                resource, id, mode,
                code, profileId, status,
                details, expiresAt, revokedAt,
                createdAt, links);
        }

    }
}
