/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * EntityBalanceStatus
 * 
 * <p>The status of the balance.
 */
public class EntityBalanceStatus {

    public static final EntityBalanceStatus ACTIVE = new EntityBalanceStatus("active");
    public static final EntityBalanceStatus INACTIVE = new EntityBalanceStatus("inactive");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, EntityBalanceStatus> values = createValuesMap();
    private static final Map<String, EntityBalanceStatusEnum> enums = createEnumsMap();

    private final String value;

    private EntityBalanceStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a EntityBalanceStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as EntityBalanceStatus
     */ 
    @JsonCreator
    public static EntityBalanceStatus of(String value) {
        synchronized (EntityBalanceStatus.class) {
            return values.computeIfAbsent(value, v -> new EntityBalanceStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<EntityBalanceStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        EntityBalanceStatus other = (EntityBalanceStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "EntityBalanceStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static EntityBalanceStatus[] values() {
        synchronized (EntityBalanceStatus.class) {
            return values.values().toArray(new EntityBalanceStatus[] {});
        }
    }

    private static final Map<String, EntityBalanceStatus> createValuesMap() {
        Map<String, EntityBalanceStatus> map = new LinkedHashMap<>();
        map.put("active", ACTIVE);
        map.put("inactive", INACTIVE);
        return map;
    }

    private static final Map<String, EntityBalanceStatusEnum> createEnumsMap() {
        Map<String, EntityBalanceStatusEnum> map = new HashMap<>();
        map.put("active", EntityBalanceStatusEnum.ACTIVE);
        map.put("inactive", EntityBalanceStatusEnum.INACTIVE);
        return map;
    }
    
    
    public enum EntityBalanceStatusEnum {

        ACTIVE("active"),
        INACTIVE("inactive"),;

        private final String value;

        private EntityBalanceStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

