/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: fb5cd4f3b847
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.String;
import java.util.Objects;
import java.util.Optional;

/**
 * SalesInvoiceStatusUpdate
 * 
 * <p>The status for the invoice to end up in.
 * 
 * <p>A `draft` invoice is not paid or not sent and can be updated after creation. Setting it to `issued`
 * sends it to
 * the recipient so they may then pay through our payment system. To skip our payment process, set this
 * to `paid` to
 * mark it as paid. It can then subsequently be sent as well, same as with `issued`.
 * 
 * <p>Dependent parameters:
 * - `paymentDetails` is required if invoice should be set directly to `paid`
 * - `customerId` and `mandateId` are required if a recurring payment should be used to set the invoice
 * to `paid`
 * - `emailDetails` optional for `issued` and `paid` to send the invoice by email
 */
public enum SalesInvoiceStatusUpdate {
    DRAFT("draft"),
    ISSUED("issued"),
    PAID("paid"),
    CANCELLED("cancelled");

    @JsonValue
    private final String value;

    SalesInvoiceStatusUpdate(String value) {
        this.value = value;
    }
    
    public String value() {
        return value;
    }
    
    public static Optional<SalesInvoiceStatusUpdate> fromValue(String value) {
        for (SalesInvoiceStatusUpdate o: SalesInvoiceStatusUpdate.values()) {
            if (Objects.deepEquals(o.value, value)) {
                return Optional.of(o);
            }
        }
        return Optional.empty();
    }
}

