/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 550c4c58a21c
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * GiftcardStatus
 * 
 * <p>The status of the issuer.
 * If the status is `pending-issuer`, an additional action from your side may be required with the
 * issuer.
 */
public class GiftcardStatus {

    public static final GiftcardStatus ACTIVATED = new GiftcardStatus("activated");
    public static final GiftcardStatus PENDING_ISSUER = new GiftcardStatus("pending-issuer");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, GiftcardStatus> values = createValuesMap();
    private static final Map<String, GiftcardStatusEnum> enums = createEnumsMap();

    private final String value;

    private GiftcardStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a GiftcardStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as GiftcardStatus
     */ 
    @JsonCreator
    public static GiftcardStatus of(String value) {
        synchronized (GiftcardStatus.class) {
            return values.computeIfAbsent(value, v -> new GiftcardStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<GiftcardStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        GiftcardStatus other = (GiftcardStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "GiftcardStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static GiftcardStatus[] values() {
        synchronized (GiftcardStatus.class) {
            return values.values().toArray(new GiftcardStatus[] {});
        }
    }

    private static final Map<String, GiftcardStatus> createValuesMap() {
        Map<String, GiftcardStatus> map = new LinkedHashMap<>();
        map.put("activated", ACTIVATED);
        map.put("pending-issuer", PENDING_ISSUER);
        return map;
    }

    private static final Map<String, GiftcardStatusEnum> createEnumsMap() {
        Map<String, GiftcardStatusEnum> map = new HashMap<>();
        map.put("activated", GiftcardStatusEnum.ACTIVATED);
        map.put("pending-issuer", GiftcardStatusEnum.PENDING_ISSUER);
        return map;
    }
    
    
    public enum GiftcardStatusEnum {

        ACTIVATED("activated"),
        PENDING_ISSUER("pending-issuer"),;

        private final String value;

        private GiftcardStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

