/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * CaptureModeResponse
 * 
 * <p>Indicate if the funds should be captured immediately or if you want to [place a
 * hold](https://docs.mollie.com/docs/place-a-hold-for-a-payment#/)
 * and capture at a later time.
 * 
 * <p>This field needs to be set to `manual` for method `riverty`.
 */
public class CaptureModeResponse {

    public static final CaptureModeResponse AUTOMATIC = new CaptureModeResponse("automatic");
    public static final CaptureModeResponse MANUAL = new CaptureModeResponse("manual");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, CaptureModeResponse> values = createValuesMap();
    private static final Map<String, CaptureModeResponseEnum> enums = createEnumsMap();

    private final String value;

    private CaptureModeResponse(String value) {
        this.value = value;
    }

    /**
     * Returns a CaptureModeResponse with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as CaptureModeResponse
     */ 
    @JsonCreator
    public static CaptureModeResponse of(String value) {
        synchronized (CaptureModeResponse.class) {
            return values.computeIfAbsent(value, v -> new CaptureModeResponse(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<CaptureModeResponseEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        CaptureModeResponse other = (CaptureModeResponse) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "CaptureModeResponse [value=" + value + "]";
    }

    // return an array just like an enum
    public static CaptureModeResponse[] values() {
        synchronized (CaptureModeResponse.class) {
            return values.values().toArray(new CaptureModeResponse[] {});
        }
    }

    private static final Map<String, CaptureModeResponse> createValuesMap() {
        Map<String, CaptureModeResponse> map = new LinkedHashMap<>();
        map.put("automatic", AUTOMATIC);
        map.put("manual", MANUAL);
        return map;
    }

    private static final Map<String, CaptureModeResponseEnum> createEnumsMap() {
        Map<String, CaptureModeResponseEnum> map = new HashMap<>();
        map.put("automatic", CaptureModeResponseEnum.AUTOMATIC);
        map.put("manual", CaptureModeResponseEnum.MANUAL);
        return map;
    }
    
    
    public enum CaptureModeResponseEnum {

        AUTOMATIC("automatic"),
        MANUAL("manual"),;

        private final String value;

        private CaptureModeResponseEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

