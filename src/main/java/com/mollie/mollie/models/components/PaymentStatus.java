/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * <p>Wrapper class for an "open" enum. "Open" enums are those that are expected
 * to evolve (particularly with the addition of enum members over time). If an
 * open enum is used then the appearance of unexpected enum values (say in a 
 * response from an updated an API) will not bring about a runtime error thus 
 * ensuring that non-updated client versions can continue to work without error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe
 * cache is maintained to ensure that). As a consequence instances created with the 
 * same value will satisfy reference equality (via {@code ==}).
 * 
 * <p>This class is intended to emulate an enum (in terms of common usage and with 
 * reference equality) but with the ability to carry unknown values. Unfortunately
 * Java does not permit the use of an instance in a switch expression but you can 
 * use the {@code asEnum()} method (after dealing with the `Optional` appropriately).
 *
 */
/**
 * PaymentStatus
 * 
 * <p>The payment's status. Refer to the [documentation regarding statuses](https://docs.mollie.com/docs/status-change#/) for more info about which
 * statuses occur at what point.
 */
@JsonDeserialize(using = PaymentStatus._Deserializer.class)
@JsonSerialize(using = PaymentStatus._Serializer.class)
public class PaymentStatus {

    public static final PaymentStatus OPEN = new PaymentStatus("open");
    public static final PaymentStatus PENDING = new PaymentStatus("pending");
    public static final PaymentStatus AUTHORIZED = new PaymentStatus("authorized");
    public static final PaymentStatus PAID = new PaymentStatus("paid");
    public static final PaymentStatus CANCELED = new PaymentStatus("canceled");
    public static final PaymentStatus EXPIRED = new PaymentStatus("expired");
    public static final PaymentStatus FAILED = new PaymentStatus("failed");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, PaymentStatus> values = createValuesMap();
    private static final Map<String, PaymentStatusEnum> enums = createEnumsMap();

    private final String value;

    private PaymentStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a PaymentStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as PaymentStatus
     */ 
    public static PaymentStatus of(String value) {
        synchronized (PaymentStatus.class) {
            return values.computeIfAbsent(value, v -> new PaymentStatus(v));
        }
    }

    public String value() {
        return value;
    }

    public Optional<PaymentStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        PaymentStatus other = (PaymentStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "PaymentStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static PaymentStatus[] values() {
        synchronized (PaymentStatus.class) {
            return values.values().toArray(new PaymentStatus[] {});
        }
    }

    private static final Map<String, PaymentStatus> createValuesMap() {
        Map<String, PaymentStatus> map = new LinkedHashMap<>();
        map.put("open", OPEN);
        map.put("pending", PENDING);
        map.put("authorized", AUTHORIZED);
        map.put("paid", PAID);
        map.put("canceled", CANCELED);
        map.put("expired", EXPIRED);
        map.put("failed", FAILED);
        return map;
    }

    private static final Map<String, PaymentStatusEnum> createEnumsMap() {
        Map<String, PaymentStatusEnum> map = new HashMap<>();
        map.put("open", PaymentStatusEnum.OPEN);
        map.put("pending", PaymentStatusEnum.PENDING);
        map.put("authorized", PaymentStatusEnum.AUTHORIZED);
        map.put("paid", PaymentStatusEnum.PAID);
        map.put("canceled", PaymentStatusEnum.CANCELED);
        map.put("expired", PaymentStatusEnum.EXPIRED);
        map.put("failed", PaymentStatusEnum.FAILED);
        return map;
    }
    
    @SuppressWarnings("serial")
    public static final class _Serializer extends StdSerializer<PaymentStatus> {

        protected _Serializer() {
            super(PaymentStatus.class);
        }

        @Override
        public void serialize(PaymentStatus value, JsonGenerator g, SerializerProvider provider)
                throws IOException, JsonProcessingException {
            g.writeObject(value.value);
        }
    }

    @SuppressWarnings("serial")
    public static final class _Deserializer extends StdDeserializer<PaymentStatus> {

        protected _Deserializer() {
            super(PaymentStatus.class);
        }

        @Override
        public PaymentStatus deserialize(JsonParser p, DeserializationContext ctxt)
                throws IOException, JacksonException {
            String v = p.readValueAs(new TypeReference<String>() {});
            // use the factory method to ensure we get singletons
            return PaymentStatus.of(v);
        }
    }
    
    public enum PaymentStatusEnum {

        OPEN("open"),
        PENDING("pending"),
        AUTHORIZED("authorized"),
        PAID("paid"),
        CANCELED("canceled"),
        EXPIRED("expired"),
        FAILED("failed"),;

        private final String value;

        private PaymentStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

