/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 67f7932b49b2
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * VoucherStatus
 * 
 * <p>The status of the issuer.
 * If the status is `pending-issuer`, an additional action from your side may be required with the
 * issuer.
 */
public class VoucherStatus {

    public static final VoucherStatus ACTIVATED = new VoucherStatus("activated");
    public static final VoucherStatus PENDING_ISSUER = new VoucherStatus("pending-issuer");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, VoucherStatus> values = createValuesMap();
    private static final Map<String, VoucherStatusEnum> enums = createEnumsMap();

    private final String value;

    private VoucherStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a VoucherStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as VoucherStatus
     */ 
    @JsonCreator
    public static VoucherStatus of(String value) {
        synchronized (VoucherStatus.class) {
            return values.computeIfAbsent(value, v -> new VoucherStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<VoucherStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        VoucherStatus other = (VoucherStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "VoucherStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static VoucherStatus[] values() {
        synchronized (VoucherStatus.class) {
            return values.values().toArray(new VoucherStatus[] {});
        }
    }

    private static final Map<String, VoucherStatus> createValuesMap() {
        Map<String, VoucherStatus> map = new LinkedHashMap<>();
        map.put("activated", ACTIVATED);
        map.put("pending-issuer", PENDING_ISSUER);
        return map;
    }

    private static final Map<String, VoucherStatusEnum> createEnumsMap() {
        Map<String, VoucherStatusEnum> map = new HashMap<>();
        map.put("activated", VoucherStatusEnum.ACTIVATED);
        map.put("pending-issuer", VoucherStatusEnum.PENDING_ISSUER);
        return map;
    }
    
    
    public enum VoucherStatusEnum {

        ACTIVATED("activated"),
        PENDING_ISSUER("pending-issuer"),;

        private final String value;

        private VoucherStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

