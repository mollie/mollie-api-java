/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * <p>Wrapper class for an "open" enum. "Open" enums are those that are expected
 * to evolve (particularly with the addition of enum members over time). If an
 * open enum is used then the appearance of unexpected enum values (say in a 
 * response from an updated an API) will not bring about a runtime error thus 
 * ensuring that non-updated client versions can continue to work without error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe
 * cache is maintained to ensure that). As a consequence instances created with the 
 * same value will satisfy reference equality (via {@code ==}).
 * 
 * <p>This class is intended to emulate an enum (in terms of common usage and with 
 * reference equality) but with the ability to carry unknown values. Unfortunately
 * Java does not permit the use of an instance in a switch expression but you can 
 * use the {@code asEnum()} method (after dealing with the `Optional` appropriately).
 *
 */
/**
 * SubscriptionStatus
 * 
 * <p>The subscription's current status is directly related to the status of the underlying customer or mandate that is
 * enabling the subscription.
 */
@JsonDeserialize(using = SubscriptionStatus._Deserializer.class)
@JsonSerialize(using = SubscriptionStatus._Serializer.class)
public class SubscriptionStatus {

    public static final SubscriptionStatus PENDING = new SubscriptionStatus("pending");
    public static final SubscriptionStatus ACTIVE = new SubscriptionStatus("active");
    public static final SubscriptionStatus CANCELED = new SubscriptionStatus("canceled");
    public static final SubscriptionStatus SUSPENDED = new SubscriptionStatus("suspended");
    public static final SubscriptionStatus COMPLETED = new SubscriptionStatus("completed");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, SubscriptionStatus> values = createValuesMap();
    private static final Map<String, SubscriptionStatusEnum> enums = createEnumsMap();

    private final String value;

    private SubscriptionStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a SubscriptionStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as SubscriptionStatus
     */ 
    public static SubscriptionStatus of(String value) {
        synchronized (SubscriptionStatus.class) {
            return values.computeIfAbsent(value, v -> new SubscriptionStatus(v));
        }
    }

    public String value() {
        return value;
    }

    public Optional<SubscriptionStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        SubscriptionStatus other = (SubscriptionStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "SubscriptionStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static SubscriptionStatus[] values() {
        synchronized (SubscriptionStatus.class) {
            return values.values().toArray(new SubscriptionStatus[] {});
        }
    }

    private static final Map<String, SubscriptionStatus> createValuesMap() {
        Map<String, SubscriptionStatus> map = new LinkedHashMap<>();
        map.put("pending", PENDING);
        map.put("active", ACTIVE);
        map.put("canceled", CANCELED);
        map.put("suspended", SUSPENDED);
        map.put("completed", COMPLETED);
        return map;
    }

    private static final Map<String, SubscriptionStatusEnum> createEnumsMap() {
        Map<String, SubscriptionStatusEnum> map = new HashMap<>();
        map.put("pending", SubscriptionStatusEnum.PENDING);
        map.put("active", SubscriptionStatusEnum.ACTIVE);
        map.put("canceled", SubscriptionStatusEnum.CANCELED);
        map.put("suspended", SubscriptionStatusEnum.SUSPENDED);
        map.put("completed", SubscriptionStatusEnum.COMPLETED);
        return map;
    }
    
    @SuppressWarnings("serial")
    public static final class _Serializer extends StdSerializer<SubscriptionStatus> {

        protected _Serializer() {
            super(SubscriptionStatus.class);
        }

        @Override
        public void serialize(SubscriptionStatus value, JsonGenerator g, SerializerProvider provider)
                throws IOException, JsonProcessingException {
            g.writeObject(value.value);
        }
    }

    @SuppressWarnings("serial")
    public static final class _Deserializer extends StdDeserializer<SubscriptionStatus> {

        protected _Deserializer() {
            super(SubscriptionStatus.class);
        }

        @Override
        public SubscriptionStatus deserialize(JsonParser p, DeserializationContext ctxt)
                throws IOException, JacksonException {
            String v = p.readValueAs(new TypeReference<String>() {});
            // use the factory method to ensure we get singletons
            return SubscriptionStatus.of(v);
        }
    }
    
    public enum SubscriptionStatusEnum {

        PENDING("pending"),
        ACTIVE("active"),
        CANCELED("canceled"),
        SUSPENDED("suspended"),
        COMPLETED("completed"),;

        private final String value;

        private SubscriptionStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

