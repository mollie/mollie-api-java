/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 585209208fca
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class RefundResponseStatus {

    public static final RefundResponseStatus QUEUED = new RefundResponseStatus("queued");
    public static final RefundResponseStatus PENDING = new RefundResponseStatus("pending");
    public static final RefundResponseStatus PROCESSING = new RefundResponseStatus("processing");
    public static final RefundResponseStatus REFUNDED = new RefundResponseStatus("refunded");
    public static final RefundResponseStatus FAILED = new RefundResponseStatus("failed");
    public static final RefundResponseStatus CANCELED = new RefundResponseStatus("canceled");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, RefundResponseStatus> values = createValuesMap();
    private static final Map<String, RefundResponseStatusEnum> enums = createEnumsMap();

    private final String value;

    private RefundResponseStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a RefundResponseStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as RefundResponseStatus
     */ 
    @JsonCreator
    public static RefundResponseStatus of(String value) {
        synchronized (RefundResponseStatus.class) {
            return values.computeIfAbsent(value, v -> new RefundResponseStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<RefundResponseStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        RefundResponseStatus other = (RefundResponseStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "RefundResponseStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static RefundResponseStatus[] values() {
        synchronized (RefundResponseStatus.class) {
            return values.values().toArray(new RefundResponseStatus[] {});
        }
    }

    private static final Map<String, RefundResponseStatus> createValuesMap() {
        Map<String, RefundResponseStatus> map = new LinkedHashMap<>();
        map.put("queued", QUEUED);
        map.put("pending", PENDING);
        map.put("processing", PROCESSING);
        map.put("refunded", REFUNDED);
        map.put("failed", FAILED);
        map.put("canceled", CANCELED);
        return map;
    }

    private static final Map<String, RefundResponseStatusEnum> createEnumsMap() {
        Map<String, RefundResponseStatusEnum> map = new HashMap<>();
        map.put("queued", RefundResponseStatusEnum.QUEUED);
        map.put("pending", RefundResponseStatusEnum.PENDING);
        map.put("processing", RefundResponseStatusEnum.PROCESSING);
        map.put("refunded", RefundResponseStatusEnum.REFUNDED);
        map.put("failed", RefundResponseStatusEnum.FAILED);
        map.put("canceled", RefundResponseStatusEnum.CANCELED);
        return map;
    }
    
    
    public enum RefundResponseStatusEnum {

        QUEUED("queued"),
        PENDING("pending"),
        PROCESSING("processing"),
        REFUNDED("refunded"),
        FAILED("failed"),
        CANCELED("canceled"),;

        private final String value;

        private RefundResponseStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

