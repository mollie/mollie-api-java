/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: beba5c60ff4c
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.mollie.mollie.utils.Utils;
import java.lang.Override;
import java.lang.String;
import java.time.LocalDate;
import java.util.Optional;
import org.openapitools.jackson.nullable.JsonNullable;


public class CreateDraftTransferRequest {
    /**
     * The IBAN of the debtor's (sender) Mollie Business Account. Defaults to your organization's primary
     * account
     * if omitted.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("debtorIban")
    private Optional<String> debtorIban;

    /**
     * A party involved in the draft transfer, representing either the debtor (sender) or creditor
     * (recipient). Contains the party's name and account details.
     */
    @JsonProperty("creditor")
    private DraftTransferParty creditor;

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    @JsonProperty("amount")
    private Amount amount;

    /**
     * A short description of the draft transfer. Once approved, this will appear on the bank statement of
     * both
     * the debtor and creditor.
     */
    @JsonInclude(Include.ALWAYS)
    @JsonProperty("description")
    private Optional<String> description;

    /**
     * The date on which the resulting transfer will be executed, once approved. Defaults to same-day if
     * omitted,
     * in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date format.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("scheduledFor")
    private JsonNullable<LocalDate> scheduledFor;

    @JsonCreator
    public CreateDraftTransferRequest(
            @JsonProperty("debtorIban") Optional<String> debtorIban,
            @JsonProperty("creditor") DraftTransferParty creditor,
            @JsonProperty("amount") Amount amount,
            @JsonProperty("description") Optional<String> description,
            @JsonProperty("scheduledFor") JsonNullable<LocalDate> scheduledFor) {
        Utils.checkNotNull(debtorIban, "debtorIban");
        Utils.checkNotNull(creditor, "creditor");
        Utils.checkNotNull(amount, "amount");
        Utils.checkNotNull(description, "description");
        Utils.checkNotNull(scheduledFor, "scheduledFor");
        this.debtorIban = debtorIban;
        this.creditor = creditor;
        this.amount = amount;
        this.description = description;
        this.scheduledFor = scheduledFor;
    }
    
    public CreateDraftTransferRequest(
            DraftTransferParty creditor,
            Amount amount) {
        this(Optional.empty(), creditor, amount,
            Optional.empty(), JsonNullable.undefined());
    }

    /**
     * The IBAN of the debtor's (sender) Mollie Business Account. Defaults to your organization's primary
     * account
     * if omitted.
     */
    @JsonIgnore
    public Optional<String> debtorIban() {
        return debtorIban;
    }

    /**
     * A party involved in the draft transfer, representing either the debtor (sender) or creditor
     * (recipient). Contains the party's name and account details.
     */
    @JsonIgnore
    public DraftTransferParty creditor() {
        return creditor;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    @JsonIgnore
    public Amount amount() {
        return amount;
    }

    /**
     * A short description of the draft transfer. Once approved, this will appear on the bank statement of
     * both
     * the debtor and creditor.
     */
    @JsonIgnore
    public Optional<String> description() {
        return description;
    }

    /**
     * The date on which the resulting transfer will be executed, once approved. Defaults to same-day if
     * omitted,
     * in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date format.
     */
    @JsonIgnore
    public JsonNullable<LocalDate> scheduledFor() {
        return scheduledFor;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * The IBAN of the debtor's (sender) Mollie Business Account. Defaults to your organization's primary
     * account
     * if omitted.
     */
    public CreateDraftTransferRequest withDebtorIban(String debtorIban) {
        Utils.checkNotNull(debtorIban, "debtorIban");
        this.debtorIban = Optional.ofNullable(debtorIban);
        return this;
    }


    /**
     * The IBAN of the debtor's (sender) Mollie Business Account. Defaults to your organization's primary
     * account
     * if omitted.
     */
    public CreateDraftTransferRequest withDebtorIban(Optional<String> debtorIban) {
        Utils.checkNotNull(debtorIban, "debtorIban");
        this.debtorIban = debtorIban;
        return this;
    }

    /**
     * A party involved in the draft transfer, representing either the debtor (sender) or creditor
     * (recipient). Contains the party's name and account details.
     */
    public CreateDraftTransferRequest withCreditor(DraftTransferParty creditor) {
        Utils.checkNotNull(creditor, "creditor");
        this.creditor = creditor;
        return this;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    public CreateDraftTransferRequest withAmount(Amount amount) {
        Utils.checkNotNull(amount, "amount");
        this.amount = amount;
        return this;
    }

    /**
     * A short description of the draft transfer. Once approved, this will appear on the bank statement of
     * both
     * the debtor and creditor.
     */
    public CreateDraftTransferRequest withDescription(String description) {
        Utils.checkNotNull(description, "description");
        this.description = Optional.ofNullable(description);
        return this;
    }


    /**
     * A short description of the draft transfer. Once approved, this will appear on the bank statement of
     * both
     * the debtor and creditor.
     */
    public CreateDraftTransferRequest withDescription(Optional<String> description) {
        Utils.checkNotNull(description, "description");
        this.description = description;
        return this;
    }

    /**
     * The date on which the resulting transfer will be executed, once approved. Defaults to same-day if
     * omitted,
     * in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date format.
     */
    public CreateDraftTransferRequest withScheduledFor(LocalDate scheduledFor) {
        Utils.checkNotNull(scheduledFor, "scheduledFor");
        this.scheduledFor = JsonNullable.of(scheduledFor);
        return this;
    }

    /**
     * The date on which the resulting transfer will be executed, once approved. Defaults to same-day if
     * omitted,
     * in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date format.
     */
    public CreateDraftTransferRequest withScheduledFor(JsonNullable<LocalDate> scheduledFor) {
        Utils.checkNotNull(scheduledFor, "scheduledFor");
        this.scheduledFor = scheduledFor;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        CreateDraftTransferRequest other = (CreateDraftTransferRequest) o;
        return 
            Utils.enhancedDeepEquals(this.debtorIban, other.debtorIban) &&
            Utils.enhancedDeepEquals(this.creditor, other.creditor) &&
            Utils.enhancedDeepEquals(this.amount, other.amount) &&
            Utils.enhancedDeepEquals(this.description, other.description) &&
            Utils.enhancedDeepEquals(this.scheduledFor, other.scheduledFor);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            debtorIban, creditor, amount,
            description, scheduledFor);
    }
    
    @Override
    public String toString() {
        return Utils.toString(CreateDraftTransferRequest.class,
                "debtorIban", debtorIban,
                "creditor", creditor,
                "amount", amount,
                "description", description,
                "scheduledFor", scheduledFor);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private Optional<String> debtorIban = Optional.empty();

        private DraftTransferParty creditor;

        private Amount amount;

        private Optional<String> description = Optional.empty();

        private JsonNullable<LocalDate> scheduledFor = JsonNullable.undefined();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * The IBAN of the debtor's (sender) Mollie Business Account. Defaults to your organization's primary
         * account
         * if omitted.
         */
        public Builder debtorIban(String debtorIban) {
            Utils.checkNotNull(debtorIban, "debtorIban");
            this.debtorIban = Optional.ofNullable(debtorIban);
            return this;
        }

        /**
         * The IBAN of the debtor's (sender) Mollie Business Account. Defaults to your organization's primary
         * account
         * if omitted.
         */
        public Builder debtorIban(Optional<String> debtorIban) {
            Utils.checkNotNull(debtorIban, "debtorIban");
            this.debtorIban = debtorIban;
            return this;
        }


        /**
         * A party involved in the draft transfer, representing either the debtor (sender) or creditor
         * (recipient). Contains the party's name and account details.
         */
        public Builder creditor(DraftTransferParty creditor) {
            Utils.checkNotNull(creditor, "creditor");
            this.creditor = creditor;
            return this;
        }


        /**
         * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
         */
        public Builder amount(Amount amount) {
            Utils.checkNotNull(amount, "amount");
            this.amount = amount;
            return this;
        }


        /**
         * A short description of the draft transfer. Once approved, this will appear on the bank statement of
         * both
         * the debtor and creditor.
         */
        public Builder description(String description) {
            Utils.checkNotNull(description, "description");
            this.description = Optional.ofNullable(description);
            return this;
        }

        /**
         * A short description of the draft transfer. Once approved, this will appear on the bank statement of
         * both
         * the debtor and creditor.
         */
        public Builder description(Optional<String> description) {
            Utils.checkNotNull(description, "description");
            this.description = description;
            return this;
        }


        /**
         * The date on which the resulting transfer will be executed, once approved. Defaults to same-day if
         * omitted,
         * in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date format.
         */
        public Builder scheduledFor(LocalDate scheduledFor) {
            Utils.checkNotNull(scheduledFor, "scheduledFor");
            this.scheduledFor = JsonNullable.of(scheduledFor);
            return this;
        }

        /**
         * The date on which the resulting transfer will be executed, once approved. Defaults to same-day if
         * omitted,
         * in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date format.
         */
        public Builder scheduledFor(JsonNullable<LocalDate> scheduledFor) {
            Utils.checkNotNull(scheduledFor, "scheduledFor");
            this.scheduledFor = scheduledFor;
            return this;
        }

        public CreateDraftTransferRequest build() {

            return new CreateDraftTransferRequest(
                debtorIban, creditor, amount,
                description, scheduledFor);
        }

    }
}
