/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * <p>Wrapper class for an "open" enum. "Open" enums are those that are expected
 * to evolve (particularly with the addition of enum members over time). If an
 * open enum is used then the appearance of unexpected enum values (say in a 
 * response from an updated an API) will not bring about a runtime error thus 
 * ensuring that non-updated client versions can continue to work without error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe
 * cache is maintained to ensure that). As a consequence instances created with the 
 * same value will satisfy reference equality (via {@code ==}).
 * 
 * <p>This class is intended to emulate an enum (in terms of common usage and with 
 * reference equality) but with the ability to carry unknown values. Unfortunately
 * Java does not permit the use of an instance in a switch expression but you can 
 * use the {@code asEnum()} method (after dealing with the `Optional` appropriately).
 *
 */
/**
 * BalanceTransferFrequency
 * 
 * <p>The frequency with which the available amount on the balance will be settled to the configured transfer
 * destination.
 * 
 * <p>Settlements created during weekends or on bank holidays will take place on the next business day.
 */
@JsonDeserialize(using = BalanceTransferFrequency._Deserializer.class)
@JsonSerialize(using = BalanceTransferFrequency._Serializer.class)
public class BalanceTransferFrequency {

    public static final BalanceTransferFrequency DAILY = new BalanceTransferFrequency("daily");
    public static final BalanceTransferFrequency EVERY_MONDAY = new BalanceTransferFrequency("every-monday");
    public static final BalanceTransferFrequency EVERY_TUESDAY = new BalanceTransferFrequency("every-tuesday");
    public static final BalanceTransferFrequency EVERY_WEDNESDAY = new BalanceTransferFrequency("every-wednesday");
    public static final BalanceTransferFrequency EVERY_THURSDAY = new BalanceTransferFrequency("every-thursday");
    public static final BalanceTransferFrequency EVERY_FRIDAY = new BalanceTransferFrequency("every-friday");
    public static final BalanceTransferFrequency MONTHLY = new BalanceTransferFrequency("monthly");
    public static final BalanceTransferFrequency NEVER = new BalanceTransferFrequency("never");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, BalanceTransferFrequency> values = createValuesMap();
    private static final Map<String, BalanceTransferFrequencyEnum> enums = createEnumsMap();

    private final String value;

    private BalanceTransferFrequency(String value) {
        this.value = value;
    }

    /**
     * Returns a BalanceTransferFrequency with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as BalanceTransferFrequency
     */ 
    public static BalanceTransferFrequency of(String value) {
        synchronized (BalanceTransferFrequency.class) {
            return values.computeIfAbsent(value, v -> new BalanceTransferFrequency(v));
        }
    }

    public String value() {
        return value;
    }

    public Optional<BalanceTransferFrequencyEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        BalanceTransferFrequency other = (BalanceTransferFrequency) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "BalanceTransferFrequency [value=" + value + "]";
    }

    // return an array just like an enum
    public static BalanceTransferFrequency[] values() {
        synchronized (BalanceTransferFrequency.class) {
            return values.values().toArray(new BalanceTransferFrequency[] {});
        }
    }

    private static final Map<String, BalanceTransferFrequency> createValuesMap() {
        Map<String, BalanceTransferFrequency> map = new LinkedHashMap<>();
        map.put("daily", DAILY);
        map.put("every-monday", EVERY_MONDAY);
        map.put("every-tuesday", EVERY_TUESDAY);
        map.put("every-wednesday", EVERY_WEDNESDAY);
        map.put("every-thursday", EVERY_THURSDAY);
        map.put("every-friday", EVERY_FRIDAY);
        map.put("monthly", MONTHLY);
        map.put("never", NEVER);
        return map;
    }

    private static final Map<String, BalanceTransferFrequencyEnum> createEnumsMap() {
        Map<String, BalanceTransferFrequencyEnum> map = new HashMap<>();
        map.put("daily", BalanceTransferFrequencyEnum.DAILY);
        map.put("every-monday", BalanceTransferFrequencyEnum.EVERY_MONDAY);
        map.put("every-tuesday", BalanceTransferFrequencyEnum.EVERY_TUESDAY);
        map.put("every-wednesday", BalanceTransferFrequencyEnum.EVERY_WEDNESDAY);
        map.put("every-thursday", BalanceTransferFrequencyEnum.EVERY_THURSDAY);
        map.put("every-friday", BalanceTransferFrequencyEnum.EVERY_FRIDAY);
        map.put("monthly", BalanceTransferFrequencyEnum.MONTHLY);
        map.put("never", BalanceTransferFrequencyEnum.NEVER);
        return map;
    }
    
    @SuppressWarnings("serial")
    public static final class _Serializer extends StdSerializer<BalanceTransferFrequency> {

        protected _Serializer() {
            super(BalanceTransferFrequency.class);
        }

        @Override
        public void serialize(BalanceTransferFrequency value, JsonGenerator g, SerializerProvider provider)
                throws IOException, JsonProcessingException {
            g.writeObject(value.value);
        }
    }

    @SuppressWarnings("serial")
    public static final class _Deserializer extends StdDeserializer<BalanceTransferFrequency> {

        protected _Deserializer() {
            super(BalanceTransferFrequency.class);
        }

        @Override
        public BalanceTransferFrequency deserialize(JsonParser p, DeserializationContext ctxt)
                throws IOException, JacksonException {
            String v = p.readValueAs(new TypeReference<String>() {});
            // use the factory method to ensure we get singletons
            return BalanceTransferFrequency.of(v);
        }
    }
    
    public enum BalanceTransferFrequencyEnum {

        DAILY("daily"),
        EVERY_MONDAY("every-monday"),
        EVERY_TUESDAY("every-tuesday"),
        EVERY_WEDNESDAY("every-wednesday"),
        EVERY_THURSDAY("every-thursday"),
        EVERY_FRIDAY("every-friday"),
        MONTHLY("monthly"),
        NEVER("never"),;

        private final String value;

        private BalanceTransferFrequencyEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

