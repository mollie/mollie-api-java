/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 814ed0e3c238
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * PayoutStatus
 * 
 * <p>The status of the payout.
 */
public class PayoutStatus {

    public static final PayoutStatus REQUESTED = new PayoutStatus("requested");
    public static final PayoutStatus INITIATED = new PayoutStatus("initiated");
    public static final PayoutStatus PROCESSING_AT_BANK = new PayoutStatus("processing-at-bank");
    public static final PayoutStatus COMPLETED = new PayoutStatus("completed");
    public static final PayoutStatus FAILED = new PayoutStatus("failed");
    public static final PayoutStatus CANCELED = new PayoutStatus("canceled");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, PayoutStatus> values = createValuesMap();
    private static final Map<String, PayoutStatusEnum> enums = createEnumsMap();

    private final String value;

    private PayoutStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a PayoutStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as PayoutStatus
     */ 
    @JsonCreator
    public static PayoutStatus of(String value) {
        synchronized (PayoutStatus.class) {
            return values.computeIfAbsent(value, v -> new PayoutStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<PayoutStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        PayoutStatus other = (PayoutStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "PayoutStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static PayoutStatus[] values() {
        synchronized (PayoutStatus.class) {
            return values.values().toArray(new PayoutStatus[] {});
        }
    }

    private static final Map<String, PayoutStatus> createValuesMap() {
        Map<String, PayoutStatus> map = new LinkedHashMap<>();
        map.put("requested", REQUESTED);
        map.put("initiated", INITIATED);
        map.put("processing-at-bank", PROCESSING_AT_BANK);
        map.put("completed", COMPLETED);
        map.put("failed", FAILED);
        map.put("canceled", CANCELED);
        return map;
    }

    private static final Map<String, PayoutStatusEnum> createEnumsMap() {
        Map<String, PayoutStatusEnum> map = new HashMap<>();
        map.put("requested", PayoutStatusEnum.REQUESTED);
        map.put("initiated", PayoutStatusEnum.INITIATED);
        map.put("processing-at-bank", PayoutStatusEnum.PROCESSING_AT_BANK);
        map.put("completed", PayoutStatusEnum.COMPLETED);
        map.put("failed", PayoutStatusEnum.FAILED);
        map.put("canceled", PayoutStatusEnum.CANCELED);
        return map;
    }
    
    
    public enum PayoutStatusEnum {

        REQUESTED("requested"),
        INITIATED("initiated"),
        PROCESSING_AT_BANK("processing-at-bank"),
        COMPLETED("completed"),
        FAILED("failed"),
        CANCELED("canceled"),;

        private final String value;

        private PayoutStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

