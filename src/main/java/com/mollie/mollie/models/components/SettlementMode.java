/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: cabfb35b4437
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * SettlementMode
 * 
 * <p>Whether this entity was created in live mode or in test mode. Settlements are always in live mode.
 */
public class SettlementMode {

    public static final SettlementMode LIVE = new SettlementMode("live");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, SettlementMode> values = createValuesMap();
    private static final Map<String, SettlementModeEnum> enums = createEnumsMap();

    private final String value;

    private SettlementMode(String value) {
        this.value = value;
    }

    /**
     * Returns a SettlementMode with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as SettlementMode
     */ 
    @JsonCreator
    public static SettlementMode of(String value) {
        synchronized (SettlementMode.class) {
            return values.computeIfAbsent(value, v -> new SettlementMode(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<SettlementModeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        SettlementMode other = (SettlementMode) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "SettlementMode [value=" + value + "]";
    }

    // return an array just like an enum
    public static SettlementMode[] values() {
        synchronized (SettlementMode.class) {
            return values.values().toArray(new SettlementMode[] {});
        }
    }

    private static final Map<String, SettlementMode> createValuesMap() {
        Map<String, SettlementMode> map = new LinkedHashMap<>();
        map.put("live", LIVE);
        return map;
    }

    private static final Map<String, SettlementModeEnum> createEnumsMap() {
        Map<String, SettlementModeEnum> map = new HashMap<>();
        map.put("live", SettlementModeEnum.LIVE);
        return map;
    }
    
    
    public enum SettlementModeEnum {

        LIVE("live"),;

        private final String value;

        private SettlementModeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

