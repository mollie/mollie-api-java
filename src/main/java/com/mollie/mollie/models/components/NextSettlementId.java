/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 7115684ceb6e
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class NextSettlementId {

    public static final NextSettlementId NEXT = new NextSettlementId("next");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, NextSettlementId> values = createValuesMap();
    private static final Map<String, NextSettlementIdEnum> enums = createEnumsMap();

    private final String value;

    private NextSettlementId(String value) {
        this.value = value;
    }

    /**
     * Returns a NextSettlementId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as NextSettlementId
     */ 
    @JsonCreator
    public static NextSettlementId of(String value) {
        synchronized (NextSettlementId.class) {
            return values.computeIfAbsent(value, v -> new NextSettlementId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<NextSettlementIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        NextSettlementId other = (NextSettlementId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "NextSettlementId [value=" + value + "]";
    }

    // return an array just like an enum
    public static NextSettlementId[] values() {
        synchronized (NextSettlementId.class) {
            return values.values().toArray(new NextSettlementId[] {});
        }
    }

    private static final Map<String, NextSettlementId> createValuesMap() {
        Map<String, NextSettlementId> map = new LinkedHashMap<>();
        map.put("next", NEXT);
        return map;
    }

    private static final Map<String, NextSettlementIdEnum> createEnumsMap() {
        Map<String, NextSettlementIdEnum> map = new HashMap<>();
        map.put("next", NextSettlementIdEnum.NEXT);
        return map;
    }
    
    
    public enum NextSettlementIdEnum {

        NEXT("next"),;

        private final String value;

        private NextSettlementIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

