/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.mollie.mollie.utils.Utils;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import org.openapitools.jackson.nullable.JsonNullable;

/**
 * DeductionDetails
 * 
 * <p>A detailed breakdown of the deductions withheld from the movement. Each field represents a specific
 * type of
 * deduction applied to the transaction. Only the applicable fields will be present.
 */
public class DeductionDetails {
    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("fees")
    private JsonNullable<? extends AmountNullable> fees;

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("commissions")
    private JsonNullable<? extends AmountNullable> commissions;

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("repayments")
    private JsonNullable<? extends AmountNullable> repayments;

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("reservations")
    private JsonNullable<? extends AmountNullable> reservations;

    @JsonCreator
    public DeductionDetails(
            @JsonProperty("fees") JsonNullable<? extends AmountNullable> fees,
            @JsonProperty("commissions") JsonNullable<? extends AmountNullable> commissions,
            @JsonProperty("repayments") JsonNullable<? extends AmountNullable> repayments,
            @JsonProperty("reservations") JsonNullable<? extends AmountNullable> reservations) {
        Utils.checkNotNull(fees, "fees");
        Utils.checkNotNull(commissions, "commissions");
        Utils.checkNotNull(repayments, "repayments");
        Utils.checkNotNull(reservations, "reservations");
        this.fees = fees;
        this.commissions = commissions;
        this.repayments = repayments;
        this.reservations = reservations;
    }
    
    public DeductionDetails() {
        this(JsonNullable.undefined(), JsonNullable.undefined(), JsonNullable.undefined(),
            JsonNullable.undefined());
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public JsonNullable<AmountNullable> fees() {
        return (JsonNullable<AmountNullable>) fees;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public JsonNullable<AmountNullable> commissions() {
        return (JsonNullable<AmountNullable>) commissions;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public JsonNullable<AmountNullable> repayments() {
        return (JsonNullable<AmountNullable>) repayments;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public JsonNullable<AmountNullable> reservations() {
        return (JsonNullable<AmountNullable>) reservations;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    public DeductionDetails withFees(AmountNullable fees) {
        Utils.checkNotNull(fees, "fees");
        this.fees = JsonNullable.of(fees);
        return this;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    public DeductionDetails withFees(JsonNullable<? extends AmountNullable> fees) {
        Utils.checkNotNull(fees, "fees");
        this.fees = fees;
        return this;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    public DeductionDetails withCommissions(AmountNullable commissions) {
        Utils.checkNotNull(commissions, "commissions");
        this.commissions = JsonNullable.of(commissions);
        return this;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    public DeductionDetails withCommissions(JsonNullable<? extends AmountNullable> commissions) {
        Utils.checkNotNull(commissions, "commissions");
        this.commissions = commissions;
        return this;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    public DeductionDetails withRepayments(AmountNullable repayments) {
        Utils.checkNotNull(repayments, "repayments");
        this.repayments = JsonNullable.of(repayments);
        return this;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    public DeductionDetails withRepayments(JsonNullable<? extends AmountNullable> repayments) {
        Utils.checkNotNull(repayments, "repayments");
        this.repayments = repayments;
        return this;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    public DeductionDetails withReservations(AmountNullable reservations) {
        Utils.checkNotNull(reservations, "reservations");
        this.reservations = JsonNullable.of(reservations);
        return this;
    }

    /**
     * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
     */
    public DeductionDetails withReservations(JsonNullable<? extends AmountNullable> reservations) {
        Utils.checkNotNull(reservations, "reservations");
        this.reservations = reservations;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        DeductionDetails other = (DeductionDetails) o;
        return 
            Utils.enhancedDeepEquals(this.fees, other.fees) &&
            Utils.enhancedDeepEquals(this.commissions, other.commissions) &&
            Utils.enhancedDeepEquals(this.repayments, other.repayments) &&
            Utils.enhancedDeepEquals(this.reservations, other.reservations);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            fees, commissions, repayments,
            reservations);
    }
    
    @Override
    public String toString() {
        return Utils.toString(DeductionDetails.class,
                "fees", fees,
                "commissions", commissions,
                "repayments", repayments,
                "reservations", reservations);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private JsonNullable<? extends AmountNullable> fees = JsonNullable.undefined();

        private JsonNullable<? extends AmountNullable> commissions = JsonNullable.undefined();

        private JsonNullable<? extends AmountNullable> repayments = JsonNullable.undefined();

        private JsonNullable<? extends AmountNullable> reservations = JsonNullable.undefined();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
         */
        public Builder fees(AmountNullable fees) {
            Utils.checkNotNull(fees, "fees");
            this.fees = JsonNullable.of(fees);
            return this;
        }

        /**
         * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
         */
        public Builder fees(JsonNullable<? extends AmountNullable> fees) {
            Utils.checkNotNull(fees, "fees");
            this.fees = fees;
            return this;
        }


        /**
         * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
         */
        public Builder commissions(AmountNullable commissions) {
            Utils.checkNotNull(commissions, "commissions");
            this.commissions = JsonNullable.of(commissions);
            return this;
        }

        /**
         * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
         */
        public Builder commissions(JsonNullable<? extends AmountNullable> commissions) {
            Utils.checkNotNull(commissions, "commissions");
            this.commissions = commissions;
            return this;
        }


        /**
         * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
         */
        public Builder repayments(AmountNullable repayments) {
            Utils.checkNotNull(repayments, "repayments");
            this.repayments = JsonNullable.of(repayments);
            return this;
        }

        /**
         * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
         */
        public Builder repayments(JsonNullable<? extends AmountNullable> repayments) {
            Utils.checkNotNull(repayments, "repayments");
            this.repayments = repayments;
            return this;
        }


        /**
         * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
         */
        public Builder reservations(AmountNullable reservations) {
            Utils.checkNotNull(reservations, "reservations");
            this.reservations = JsonNullable.of(reservations);
            return this;
        }

        /**
         * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
         */
        public Builder reservations(JsonNullable<? extends AmountNullable> reservations) {
            Utils.checkNotNull(reservations, "reservations");
            this.reservations = reservations;
            return this;
        }

        public DeductionDetails build() {

            return new DeductionDetails(
                fees, commissions, repayments,
                reservations);
        }

    }
}
