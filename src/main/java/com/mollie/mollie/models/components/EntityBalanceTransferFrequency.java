/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.mollie.mollie.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * <p>Wrapper class for an "open" enum. "Open" enums are those that are expected
 * to evolve (particularly with the addition of enum members over time). If an
 * open enum is used then the appearance of unexpected enum values (say in a 
 * response from an updated an API) will not bring about a runtime error thus 
 * ensuring that non-updated client versions can continue to work without error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe
 * cache is maintained to ensure that). As a consequence instances created with the 
 * same value will satisfy reference equality (via {@code ==}).
 * 
 * <p>This class is intended to emulate an enum (in terms of common usage and with 
 * reference equality) but with the ability to carry unknown values. Unfortunately
 * Java does not permit the use of an instance in a switch expression but you can 
 * use the {@code asEnum()} method (after dealing with the `Optional` appropriately).
 *
 */
/**
 * EntityBalanceTransferFrequency
 * 
 * <p>The frequency with which the available amount on the balance will be settled to the configured
 * transfer
 * destination.
 * 
 * <p>Settlements created during weekends or on bank holidays will take place on the next business day.
 */
@JsonDeserialize(using = EntityBalanceTransferFrequency._Deserializer.class)
@JsonSerialize(using = EntityBalanceTransferFrequency._Serializer.class)
public class EntityBalanceTransferFrequency {

    public static final EntityBalanceTransferFrequency EVERY_DAY = new EntityBalanceTransferFrequency("every-day");
    public static final EntityBalanceTransferFrequency DAILY = new EntityBalanceTransferFrequency("daily");
    public static final EntityBalanceTransferFrequency EVERY_MONDAY = new EntityBalanceTransferFrequency("every-monday");
    public static final EntityBalanceTransferFrequency EVERY_TUESDAY = new EntityBalanceTransferFrequency("every-tuesday");
    public static final EntityBalanceTransferFrequency EVERY_WEDNESDAY = new EntityBalanceTransferFrequency("every-wednesday");
    public static final EntityBalanceTransferFrequency EVERY_THURSDAY = new EntityBalanceTransferFrequency("every-thursday");
    public static final EntityBalanceTransferFrequency EVERY_FRIDAY = new EntityBalanceTransferFrequency("every-friday");
    public static final EntityBalanceTransferFrequency MONTHLY = new EntityBalanceTransferFrequency("monthly");
    public static final EntityBalanceTransferFrequency REVENUE_DAY = new EntityBalanceTransferFrequency("revenue-day");
    public static final EntityBalanceTransferFrequency NEVER = new EntityBalanceTransferFrequency("never");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, EntityBalanceTransferFrequency> values = createValuesMap();
    private static final Map<String, EntityBalanceTransferFrequencyEnum> enums = createEnumsMap();

    private final String value;

    private EntityBalanceTransferFrequency(String value) {
        this.value = value;
    }

    /**
     * Returns a EntityBalanceTransferFrequency with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as EntityBalanceTransferFrequency
     */ 
    public static EntityBalanceTransferFrequency of(String value) {
        synchronized (EntityBalanceTransferFrequency.class) {
            return values.computeIfAbsent(value, v -> new EntityBalanceTransferFrequency(v));
        }
    }

    public String value() {
        return value;
    }

    public Optional<EntityBalanceTransferFrequencyEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        EntityBalanceTransferFrequency other = (EntityBalanceTransferFrequency) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "EntityBalanceTransferFrequency [value=" + value + "]";
    }

    // return an array just like an enum
    public static EntityBalanceTransferFrequency[] values() {
        synchronized (EntityBalanceTransferFrequency.class) {
            return values.values().toArray(new EntityBalanceTransferFrequency[] {});
        }
    }

    private static final Map<String, EntityBalanceTransferFrequency> createValuesMap() {
        Map<String, EntityBalanceTransferFrequency> map = new LinkedHashMap<>();
        map.put("every-day", EVERY_DAY);
        map.put("daily", DAILY);
        map.put("every-monday", EVERY_MONDAY);
        map.put("every-tuesday", EVERY_TUESDAY);
        map.put("every-wednesday", EVERY_WEDNESDAY);
        map.put("every-thursday", EVERY_THURSDAY);
        map.put("every-friday", EVERY_FRIDAY);
        map.put("monthly", MONTHLY);
        map.put("revenue-day", REVENUE_DAY);
        map.put("never", NEVER);
        return map;
    }

    private static final Map<String, EntityBalanceTransferFrequencyEnum> createEnumsMap() {
        Map<String, EntityBalanceTransferFrequencyEnum> map = new HashMap<>();
        map.put("every-day", EntityBalanceTransferFrequencyEnum.EVERY_DAY);
        map.put("daily", EntityBalanceTransferFrequencyEnum.DAILY);
        map.put("every-monday", EntityBalanceTransferFrequencyEnum.EVERY_MONDAY);
        map.put("every-tuesday", EntityBalanceTransferFrequencyEnum.EVERY_TUESDAY);
        map.put("every-wednesday", EntityBalanceTransferFrequencyEnum.EVERY_WEDNESDAY);
        map.put("every-thursday", EntityBalanceTransferFrequencyEnum.EVERY_THURSDAY);
        map.put("every-friday", EntityBalanceTransferFrequencyEnum.EVERY_FRIDAY);
        map.put("monthly", EntityBalanceTransferFrequencyEnum.MONTHLY);
        map.put("revenue-day", EntityBalanceTransferFrequencyEnum.REVENUE_DAY);
        map.put("never", EntityBalanceTransferFrequencyEnum.NEVER);
        return map;
    }
    
    @SuppressWarnings("serial")
    public static final class _Serializer extends StdSerializer<EntityBalanceTransferFrequency> {

        protected _Serializer() {
            super(EntityBalanceTransferFrequency.class);
        }

        @Override
        public void serialize(EntityBalanceTransferFrequency value, JsonGenerator g, SerializerProvider provider)
                throws IOException, JsonProcessingException {
            g.writeObject(value.value);
        }
    }

    @SuppressWarnings("serial")
    public static final class _Deserializer extends StdDeserializer<EntityBalanceTransferFrequency> {

        protected _Deserializer() {
            super(EntityBalanceTransferFrequency.class);
        }

        @Override
        public EntityBalanceTransferFrequency deserialize(JsonParser p, DeserializationContext ctxt)
                throws IOException, JacksonException {
            String v = p.readValueAs(new TypeReference<String>() {});
            // use the factory method to ensure we get singletons
            return EntityBalanceTransferFrequency.of(v);
        }
    }
    
    public enum EntityBalanceTransferFrequencyEnum {

        EVERY_DAY("every-day"),
        DAILY("daily"),
        EVERY_MONDAY("every-monday"),
        EVERY_TUESDAY("every-tuesday"),
        EVERY_WEDNESDAY("every-wednesday"),
        EVERY_THURSDAY("every-thursday"),
        EVERY_FRIDAY("every-friday"),
        MONTHLY("monthly"),
        REVENUE_DAY("revenue-day"),
        NEVER("never"),;

        private final String value;

        private EntityBalanceTransferFrequencyEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

