/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 95599902b698
 */
package com.mollie.mollie;

import static com.mollie.mollie.operations.Operations.RequestOperation;

import com.mollie.mollie.models.components.CreateDraftTransferRequest;
import com.mollie.mollie.models.operations.CancelDraftTransferRequest;
import com.mollie.mollie.models.operations.CancelDraftTransferRequestBuilder;
import com.mollie.mollie.models.operations.CancelDraftTransferResponse;
import com.mollie.mollie.models.operations.CreateDraftTransferRequestBuilder;
import com.mollie.mollie.models.operations.CreateDraftTransferResponse;
import com.mollie.mollie.models.operations.GetDraftTransferRequest;
import com.mollie.mollie.models.operations.GetDraftTransferRequestBuilder;
import com.mollie.mollie.models.operations.GetDraftTransferResponse;
import com.mollie.mollie.models.operations.ListDraftTransfersRequest;
import com.mollie.mollie.models.operations.ListDraftTransfersRequestBuilder;
import com.mollie.mollie.models.operations.ListDraftTransfersResponse;
import com.mollie.mollie.operations.CancelDraftTransfer;
import com.mollie.mollie.operations.CreateDraftTransfer;
import com.mollie.mollie.operations.GetDraftTransfer;
import com.mollie.mollie.operations.ListDraftTransfers;
import com.mollie.mollie.utils.Headers;
import com.mollie.mollie.utils.Options;
import java.lang.String;
import java.util.Optional;


public class DraftTransfers {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncDraftTransfers asyncSDK;

    DraftTransfers(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncDraftTransfers(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncDraftTransfers async() {
        return asyncSDK;
    }

    /**
     * Create draft transfer
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Creates a draft transfer. The draft transfer immediately enters `awaiting-initiation` and appears in
     * the
     * initiator's queue in Mollie Apps. It carries no legal weight and moves no funds until a human
     * initiator
     * approves it there.
     * 
     * <p>### Test mode
     * 
     * <p>| Action | Test-mode behavior |
     * |---|---|
     * | Create | Always returns a synthetic draft in `awaiting-initiation`, regardless of most input
     * values |
     * | Get / List | Always returns synthetic `awaiting-initiation` draft(s), not your real data |
     * | Cancel | Always returns a synthetic `declined` draft |
     * 
     * <p>There is currently no way to synthesize an `initiated` draft via test mode alone. Initiation only
     * happens when
     * a real initiator approves in Mollie Apps, which test mode doesn't touch.
     * 
     * <p>If set, this operation will use either Security#advancedAccessToken or Security#oAuth from the
     * global security.
     * 
     * @return The call builder
     */
    public CreateDraftTransferRequestBuilder create() {
        return new CreateDraftTransferRequestBuilder(sdkConfiguration);
    }

    /**
     * Create draft transfer
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Creates a draft transfer. The draft transfer immediately enters `awaiting-initiation` and appears in
     * the
     * initiator's queue in Mollie Apps. It carries no legal weight and moves no funds until a human
     * initiator
     * approves it there.
     * 
     * <p>### Test mode
     * 
     * <p>| Action | Test-mode behavior |
     * |---|---|
     * | Create | Always returns a synthetic draft in `awaiting-initiation`, regardless of most input
     * values |
     * | Get / List | Always returns synthetic `awaiting-initiation` draft(s), not your real data |
     * | Cancel | Always returns a synthetic `declined` draft |
     * 
     * <p>There is currently no way to synthesize an `initiated` draft via test mode alone. Initiation only
     * happens when
     * a real initiator approves in Mollie Apps, which test mode doesn't touch.
     * 
     * <p>If set, this operation will use either Security#advancedAccessToken or Security#oAuth from the
     * global security.
     * 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public CreateDraftTransferResponse createDirect() {
        return create(Optional.empty(), Optional.empty(), Optional.empty());
    }

    /**
     * Create draft transfer
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Creates a draft transfer. The draft transfer immediately enters `awaiting-initiation` and appears in
     * the
     * initiator's queue in Mollie Apps. It carries no legal weight and moves no funds until a human
     * initiator
     * approves it there.
     * 
     * <p>### Test mode
     * 
     * <p>| Action | Test-mode behavior |
     * |---|---|
     * | Create | Always returns a synthetic draft in `awaiting-initiation`, regardless of most input
     * values |
     * | Get / List | Always returns synthetic `awaiting-initiation` draft(s), not your real data |
     * | Cancel | Always returns a synthetic `declined` draft |
     * 
     * <p>There is currently no way to synthesize an `initiated` draft via test mode alone. Initiation only
     * happens when
     * a real initiator approves in Mollie Apps, which test mode doesn't touch.
     * 
     * <p>If set, this operation will use either Security#advancedAccessToken or Security#oAuth from the
     * global security.
     * 
     * @param idempotencyKey A unique key to ensure idempotent requests. This key should be a UUID v4 string.
     * @param createDraftTransferRequest 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public CreateDraftTransferResponse create(
            Optional<String> idempotencyKey, Optional<? extends CreateDraftTransferRequest> createDraftTransferRequest,
            Optional<Options> options) {
        com.mollie.mollie.models.operations.CreateDraftTransferRequest request =
            com.mollie.mollie.models.operations.CreateDraftTransferRequest
                .builder()
                .idempotencyKey(idempotencyKey)
                .createDraftTransferRequest(createDraftTransferRequest)
                .build();
        RequestOperation<com.mollie.mollie.models.operations.CreateDraftTransferRequest, CreateDraftTransferResponse> operation
              = new CreateDraftTransfer.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * List draft transfers
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Retrieves a list of draft transfers for the organization including ones created in Mollie Apps, not
     * just
     * ones created via this API.
     * 
     * <p>The results are paginated.
     * 
     * <p>In test mode, this always returns a synthetic `awaiting-initiation` list. See [Create draft
     * transfer](create-draft-transfer) for full test-mode behavior.
     * 
     * <p>If set, this operation will use either Security#advancedAccessToken or Security#oAuth from the
     * global security.
     * 
     * @return The call builder
     */
    public ListDraftTransfersRequestBuilder list() {
        return new ListDraftTransfersRequestBuilder(sdkConfiguration);
    }

    /**
     * List draft transfers
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Retrieves a list of draft transfers for the organization including ones created in Mollie Apps, not
     * just
     * ones created via this API.
     * 
     * <p>The results are paginated.
     * 
     * <p>In test mode, this always returns a synthetic `awaiting-initiation` list. See [Create draft
     * transfer](create-draft-transfer) for full test-mode behavior.
     * 
     * <p>If set, this operation will use either Security#advancedAccessToken or Security#oAuth from the
     * global security.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListDraftTransfersResponse list(ListDraftTransfersRequest request) {
        return list(request, Optional.empty());
    }

    /**
     * List draft transfers
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Retrieves a list of draft transfers for the organization including ones created in Mollie Apps, not
     * just
     * ones created via this API.
     * 
     * <p>The results are paginated.
     * 
     * <p>In test mode, this always returns a synthetic `awaiting-initiation` list. See [Create draft
     * transfer](create-draft-transfer) for full test-mode behavior.
     * 
     * <p>If set, this operation will use either Security#advancedAccessToken or Security#oAuth from the
     * global security.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListDraftTransfersResponse list(ListDraftTransfersRequest request, Optional<Options> options) {
        ListDraftTransfers.Sync operation
              = new ListDraftTransfers.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Get draft transfer
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Retrieves a single draft transfer by its identifier.
     * 
     * <p>Only draft transfers created via this API are visible via this endpoint. Draft transfers created in
     * Mollie
     * Apps return a `404`, even though they appear in the [list endpoint](list-draft-transfers).
     * 
     * <p>In test mode, this always returns a synthetic `awaiting-initiation` draft. See [Create draft
     * transfer](create-draft-transfer) for full test-mode behavior.
     * 
     * <p>If set, this operation will use either Security#advancedAccessToken or Security#oAuth from the
     * global security.
     * 
     * @return The call builder
     */
    public GetDraftTransferRequestBuilder get() {
        return new GetDraftTransferRequestBuilder(sdkConfiguration);
    }

    /**
     * Get draft transfer
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Retrieves a single draft transfer by its identifier.
     * 
     * <p>Only draft transfers created via this API are visible via this endpoint. Draft transfers created in
     * Mollie
     * Apps return a `404`, even though they appear in the [list endpoint](list-draft-transfers).
     * 
     * <p>In test mode, this always returns a synthetic `awaiting-initiation` draft. See [Create draft
     * transfer](create-draft-transfer) for full test-mode behavior.
     * 
     * <p>If set, this operation will use either Security#advancedAccessToken or Security#oAuth from the
     * global security.
     * 
     * @param draftTransferId Provide the ID of the related draft transfer.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetDraftTransferResponse get(String draftTransferId) {
        return get(draftTransferId, Optional.empty(), Optional.empty());
    }

    /**
     * Get draft transfer
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Retrieves a single draft transfer by its identifier.
     * 
     * <p>Only draft transfers created via this API are visible via this endpoint. Draft transfers created in
     * Mollie
     * Apps return a `404`, even though they appear in the [list endpoint](list-draft-transfers).
     * 
     * <p>In test mode, this always returns a synthetic `awaiting-initiation` draft. See [Create draft
     * transfer](create-draft-transfer) for full test-mode behavior.
     * 
     * <p>If set, this operation will use either Security#advancedAccessToken or Security#oAuth from the
     * global security.
     * 
     * @param draftTransferId Provide the ID of the related draft transfer.
     * @param idempotencyKey A unique key to ensure idempotent requests. This key should be a UUID v4 string.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetDraftTransferResponse get(
            String draftTransferId, Optional<String> idempotencyKey,
            Optional<Options> options) {
        GetDraftTransferRequest request =
            GetDraftTransferRequest
                .builder()
                .draftTransferId(draftTransferId)
                .idempotencyKey(idempotencyKey)
                .build();
        RequestOperation<GetDraftTransferRequest, GetDraftTransferResponse> operation
              = new GetDraftTransfer.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Cancel draft transfer
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Cancels a draft transfer created via this API. Transitions the draft transfer to `declined` with
     * `statusReason` set to `"Declined by creator"`.
     * 
     * <p>Only draft transfers created via this API, and still in `awaiting-initiation`, can be cancelled this
     * way. A
     * `422` is returned if the initiator has already started approving it.
     * 
     * <p>In test mode, this always returns a synthetic `declined` draft. See [Create draft
     * transfer](create-draft-transfer) for full test-mode behavior.
     * 
     * <p>If set, this operation will use either Security#advancedAccessToken or Security#oAuth from the
     * global security.
     * 
     * @return The call builder
     */
    public CancelDraftTransferRequestBuilder cancel() {
        return new CancelDraftTransferRequestBuilder(sdkConfiguration);
    }

    /**
     * Cancel draft transfer
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Cancels a draft transfer created via this API. Transitions the draft transfer to `declined` with
     * `statusReason` set to `"Declined by creator"`.
     * 
     * <p>Only draft transfers created via this API, and still in `awaiting-initiation`, can be cancelled this
     * way. A
     * `422` is returned if the initiator has already started approving it.
     * 
     * <p>In test mode, this always returns a synthetic `declined` draft. See [Create draft
     * transfer](create-draft-transfer) for full test-mode behavior.
     * 
     * <p>If set, this operation will use either Security#advancedAccessToken or Security#oAuth from the
     * global security.
     * 
     * @param draftTransferId Provide the ID of the related draft transfer.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public CancelDraftTransferResponse cancel(String draftTransferId) {
        return cancel(draftTransferId, Optional.empty(), Optional.empty());
    }

    /**
     * Cancel draft transfer
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Cancels a draft transfer created via this API. Transitions the draft transfer to `declined` with
     * `statusReason` set to `"Declined by creator"`.
     * 
     * <p>Only draft transfers created via this API, and still in `awaiting-initiation`, can be cancelled this
     * way. A
     * `422` is returned if the initiator has already started approving it.
     * 
     * <p>In test mode, this always returns a synthetic `declined` draft. See [Create draft
     * transfer](create-draft-transfer) for full test-mode behavior.
     * 
     * <p>If set, this operation will use either Security#advancedAccessToken or Security#oAuth from the
     * global security.
     * 
     * @param draftTransferId Provide the ID of the related draft transfer.
     * @param idempotencyKey A unique key to ensure idempotent requests. This key should be a UUID v4 string.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public CancelDraftTransferResponse cancel(
            String draftTransferId, Optional<String> idempotencyKey,
            Optional<Options> options) {
        CancelDraftTransferRequest request =
            CancelDraftTransferRequest
                .builder()
                .draftTransferId(draftTransferId)
                .idempotencyKey(idempotencyKey)
                .build();
        RequestOperation<CancelDraftTransferRequest, CancelDraftTransferResponse> operation
              = new CancelDraftTransfer.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
