/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: b70e8d296421
 */
package com.mollie.mollie;

import static com.mollie.mollie.operations.Operations.AsyncRequestOperation;

import com.mollie.mollie.models.operations.CreateTransferRequest;
import com.mollie.mollie.models.operations.GetTransferRequest;
import com.mollie.mollie.models.operations.async.CreateTransferRequestBuilder;
import com.mollie.mollie.models.operations.async.CreateTransferResponse;
import com.mollie.mollie.models.operations.async.GetTransferRequestBuilder;
import com.mollie.mollie.models.operations.async.GetTransferResponse;
import com.mollie.mollie.operations.CreateTransfer;
import com.mollie.mollie.operations.GetTransfer;
import com.mollie.mollie.utils.Headers;
import com.mollie.mollie.utils.Options;
import java.lang.Boolean;
import java.lang.String;
import java.util.Optional;
import java.util.concurrent.CompletableFuture;


public class AsyncTransfers {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final Transfers syncSDK;

    AsyncTransfers(Transfers syncSDK, SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.syncSDK = syncSDK;
    }

    /**
     * Switches to the sync SDK.
     * 
     * @return The sync SDK
     */
    public Transfers sync() {
        return syncSDK;
    }


    /**
     * Create transfer
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Create a SEPA Credit Transfer from your Mollie Business Account.
     * 
     * <p>To initiate a transfer, you must provide the transfer scheme, the amount, the debtor IBAN (your
     * Mollie Business
     * Account IBAN), and the creditor (recipient) details.
     * 
     * <p>Each request must include an `Idempotency-Key` header to prevent duplicate transfers, and must be
     * signed using the
     * `X-Client-Signature` and `X-Client-Signed-At` headers.
     * 
     * <p>### Simulating transfer scenarios in test mode
     * 
     * <p>In test mode, you can simulate various transfer scenarios by adjusting the transfer amount. This
     * allows you to
     * mimic the typical status progression of a real-world transfer. Note that a transfer's progression
     * will stop once
     * it reaches a final status: `blocked`, `failed`, or `processed`.
     * 
     * <p>| Amount  | Scenario                                           | Webhook sequence
     * |
     * |---------|----------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------|
     * | `11.00` | Transfer initiated, pending review by Mollie       |
     * `business-account-transfer.requested` → `business-account-transfer.initiated` →
     * `business-account-transfer.pending-review`                                         |
     * | `12.00` | Transfer initiated, blocked by Mollie              |
     * `business-account-transfer.requested` → `business-account-transfer.initiated` →
     * `business-account-transfer.pending-review` → `business-account-transfer.blocked`   |
     * | `13.00` | Transfer initiated, failed on scheme submission    |
     * `business-account-transfer.requested` → `business-account-transfer.initiated` →
     * `business-account-transfer.failed`                                                 |
     * | Other   | Default: transfer is processed                     |
     * `business-account-transfer.requested` → `business-account-transfer.initiated` →
     * `business-account-transfer.processed`                                              |
     * 
     * <p>This operation requires Security#organizationAccessToken to be set via the {@code security} builder
     * method when initializing the SDK.
     * 
     * @return The async call builder
     */
    public CreateTransferRequestBuilder create() {
        return new CreateTransferRequestBuilder(sdkConfiguration);
    }

    /**
     * Create transfer
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Create a SEPA Credit Transfer from your Mollie Business Account.
     * 
     * <p>To initiate a transfer, you must provide the transfer scheme, the amount, the debtor IBAN (your
     * Mollie Business
     * Account IBAN), and the creditor (recipient) details.
     * 
     * <p>Each request must include an `Idempotency-Key` header to prevent duplicate transfers, and must be
     * signed using the
     * `X-Client-Signature` and `X-Client-Signed-At` headers.
     * 
     * <p>### Simulating transfer scenarios in test mode
     * 
     * <p>In test mode, you can simulate various transfer scenarios by adjusting the transfer amount. This
     * allows you to
     * mimic the typical status progression of a real-world transfer. Note that a transfer's progression
     * will stop once
     * it reaches a final status: `blocked`, `failed`, or `processed`.
     * 
     * <p>| Amount  | Scenario                                           | Webhook sequence
     * |
     * |---------|----------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------|
     * | `11.00` | Transfer initiated, pending review by Mollie       |
     * `business-account-transfer.requested` → `business-account-transfer.initiated` →
     * `business-account-transfer.pending-review`                                         |
     * | `12.00` | Transfer initiated, blocked by Mollie              |
     * `business-account-transfer.requested` → `business-account-transfer.initiated` →
     * `business-account-transfer.pending-review` → `business-account-transfer.blocked`   |
     * | `13.00` | Transfer initiated, failed on scheme submission    |
     * `business-account-transfer.requested` → `business-account-transfer.initiated` →
     * `business-account-transfer.failed`                                                 |
     * | Other   | Default: transfer is processed                     |
     * `business-account-transfer.requested` → `business-account-transfer.initiated` →
     * `business-account-transfer.processed`                                              |
     * 
     * <p>This operation requires Security#organizationAccessToken to be set via the {@code security} builder
     * method when initializing the SDK.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return {@code CompletableFuture<CreateTransferResponse>} - The async response
     */
    public CompletableFuture<CreateTransferResponse> create(CreateTransferRequest request) {
        return create(request, Optional.empty());
    }

    /**
     * Create transfer
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Create a SEPA Credit Transfer from your Mollie Business Account.
     * 
     * <p>To initiate a transfer, you must provide the transfer scheme, the amount, the debtor IBAN (your
     * Mollie Business
     * Account IBAN), and the creditor (recipient) details.
     * 
     * <p>Each request must include an `Idempotency-Key` header to prevent duplicate transfers, and must be
     * signed using the
     * `X-Client-Signature` and `X-Client-Signed-At` headers.
     * 
     * <p>### Simulating transfer scenarios in test mode
     * 
     * <p>In test mode, you can simulate various transfer scenarios by adjusting the transfer amount. This
     * allows you to
     * mimic the typical status progression of a real-world transfer. Note that a transfer's progression
     * will stop once
     * it reaches a final status: `blocked`, `failed`, or `processed`.
     * 
     * <p>| Amount  | Scenario                                           | Webhook sequence
     * |
     * |---------|----------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------|
     * | `11.00` | Transfer initiated, pending review by Mollie       |
     * `business-account-transfer.requested` → `business-account-transfer.initiated` →
     * `business-account-transfer.pending-review`                                         |
     * | `12.00` | Transfer initiated, blocked by Mollie              |
     * `business-account-transfer.requested` → `business-account-transfer.initiated` →
     * `business-account-transfer.pending-review` → `business-account-transfer.blocked`   |
     * | `13.00` | Transfer initiated, failed on scheme submission    |
     * `business-account-transfer.requested` → `business-account-transfer.initiated` →
     * `business-account-transfer.failed`                                                 |
     * | Other   | Default: transfer is processed                     |
     * `business-account-transfer.requested` → `business-account-transfer.initiated` →
     * `business-account-transfer.processed`                                              |
     * 
     * <p>This operation requires Security#organizationAccessToken to be set via the {@code security} builder
     * method when initializing the SDK.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return {@code CompletableFuture<CreateTransferResponse>} - The async response
     */
    public CompletableFuture<CreateTransferResponse> create(CreateTransferRequest request, Optional<Options> options) {
        AsyncRequestOperation<CreateTransferRequest, CreateTransferResponse> operation
              = new CreateTransfer.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Get transfer
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Retrieve a single transfer object by its transfer ID. This allows you to check the current status
     * and details of a previously created transfer.
     * 
     * <p>This operation requires Security#organizationAccessToken to be set via the {@code security} builder
     * method when initializing the SDK.
     * 
     * @return The async call builder
     */
    public GetTransferRequestBuilder get() {
        return new GetTransferRequestBuilder(sdkConfiguration);
    }

    /**
     * Get transfer
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Retrieve a single transfer object by its transfer ID. This allows you to check the current status
     * and details of a previously created transfer.
     * 
     * <p>This operation requires Security#organizationAccessToken to be set via the {@code security} builder
     * method when initializing the SDK.
     * 
     * @param businessAccountsTransferId Provide the ID of the related transfer.
     * @return {@code CompletableFuture<GetTransferResponse>} - The async response
     */
    public CompletableFuture<GetTransferResponse> get(String businessAccountsTransferId) {
        return get(
                businessAccountsTransferId, Optional.empty(), Optional.empty(),
                Optional.empty());
    }

    /**
     * Get transfer
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Retrieve a single transfer object by its transfer ID. This allows you to check the current status
     * and details of a previously created transfer.
     * 
     * <p>This operation requires Security#organizationAccessToken to be set via the {@code security} builder
     * method when initializing the SDK.
     * 
     * @param businessAccountsTransferId Provide the ID of the related transfer.
     * @param testmode Most API credentials are specifically created for either live mode or test mode. In those cases the `testmode` query
     *         parameter must not be sent. For organization-level credentials such as OAuth access tokens, you can enable test mode by
     *         setting the `testmode` query parameter to `true`.
     *         
     *         Test entities cannot be retrieved when the endpoint is set to live mode, and vice versa.
     * @param idempotencyKey A unique key to ensure idempotent requests. This key should be a UUID v4 string.
     * @param options additional options
     * @return {@code CompletableFuture<GetTransferResponse>} - The async response
     */
    public CompletableFuture<GetTransferResponse> get(
            String businessAccountsTransferId, Optional<Boolean> testmode,
            Optional<String> idempotencyKey, Optional<Options> options) {
        GetTransferRequest request =
            GetTransferRequest
                .builder()
                .businessAccountsTransferId(businessAccountsTransferId)
                .testmode(testmode)
                .idempotencyKey(idempotencyKey)
                .build();
        AsyncRequestOperation<GetTransferRequest, GetTransferResponse> operation
              = new GetTransfer.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }

}
