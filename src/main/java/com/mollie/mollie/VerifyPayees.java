/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 6ea82eae37ed
 */
package com.mollie.mollie;

import static com.mollie.mollie.operations.Operations.RequestOperation;

import com.mollie.mollie.models.components.VerificationOfPayeeRequest;
import com.mollie.mollie.models.operations.VerifyPayeeRequest;
import com.mollie.mollie.models.operations.VerifyPayeeRequestBuilder;
import com.mollie.mollie.models.operations.VerifyPayeeResponse;
import com.mollie.mollie.operations.VerifyPayee;
import com.mollie.mollie.utils.Headers;
import com.mollie.mollie.utils.Options;
import java.lang.String;
import java.util.Optional;


public class VerifyPayees {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncVerifyPayees asyncSDK;

    VerifyPayees(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncVerifyPayees(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncVerifyPayees async() {
        return asyncSDK;
    }

    /**
     * Verify Payee
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Perform a Verification of Payee (VoP) check. This allows you to verify the account holder name
     * against the
     * records held by the receiving bank before initiating a transfer.
     * 
     * <p>The verification result indicates whether the provided name matches, closely matches, or does not
     * match the
     * name on file at the receiving bank. This helps prevent misdirected payments.
     * 
     * <p>### Simulating verification scenarios in test mode
     * 
     * <p>In test mode, you can simulate various verification outcomes by adjusting the creditor name in the
     * `creditorBankAccount.accountHolderName` property. This allows you to test all possible Verification
     * of Payee
     * results without needing special properties. The names are case insensitive.
     * 
     * <p>| Account holder name                    | Scenario                                      |
     * Verification result | Suggested name |
     * |----------------------------------------|-----------------------------------------------|---------------------|----------------|
     * | `John Close Match`                     | Name closely matches the bank records          |
     * `close-match`       | `John Match`   |
     * | `John No Match`                        | Name does not match the bank records           |
     * `no-match`          | —              |
     * | `John Unavailable`                     | Verification is not available                  |
     * `not-available`     | —              |
     * | Any other name                         | Default: name matches the bank records         | `match`
     * | —              |
     * 
     * <p>This operation requires Security#organizationAccessToken to be set via the {@code security} builder
     * method when initializing the SDK.
     * 
     * @return The call builder
     */
    public VerifyPayeeRequestBuilder create() {
        return new VerifyPayeeRequestBuilder(sdkConfiguration);
    }

    /**
     * Verify Payee
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Perform a Verification of Payee (VoP) check. This allows you to verify the account holder name
     * against the
     * records held by the receiving bank before initiating a transfer.
     * 
     * <p>The verification result indicates whether the provided name matches, closely matches, or does not
     * match the
     * name on file at the receiving bank. This helps prevent misdirected payments.
     * 
     * <p>### Simulating verification scenarios in test mode
     * 
     * <p>In test mode, you can simulate various verification outcomes by adjusting the creditor name in the
     * `creditorBankAccount.accountHolderName` property. This allows you to test all possible Verification
     * of Payee
     * results without needing special properties. The names are case insensitive.
     * 
     * <p>| Account holder name                    | Scenario                                      |
     * Verification result | Suggested name |
     * |----------------------------------------|-----------------------------------------------|---------------------|----------------|
     * | `John Close Match`                     | Name closely matches the bank records          |
     * `close-match`       | `John Match`   |
     * | `John No Match`                        | Name does not match the bank records           |
     * `no-match`          | —              |
     * | `John Unavailable`                     | Verification is not available                  |
     * `not-available`     | —              |
     * | Any other name                         | Default: name matches the bank records         | `match`
     * | —              |
     * 
     * <p>This operation requires Security#organizationAccessToken to be set via the {@code security} builder
     * method when initializing the SDK.
     * 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public VerifyPayeeResponse createDirect() {
        return create(Optional.empty(), Optional.empty(), Optional.empty());
    }

    /**
     * Verify Payee
     * 
     * <p>&gt; 🚧 Beta feature
     * &gt;
     * &gt; This feature is currently in beta testing, and the final specification may still change.
     * 
     * <p>Perform a Verification of Payee (VoP) check. This allows you to verify the account holder name
     * against the
     * records held by the receiving bank before initiating a transfer.
     * 
     * <p>The verification result indicates whether the provided name matches, closely matches, or does not
     * match the
     * name on file at the receiving bank. This helps prevent misdirected payments.
     * 
     * <p>### Simulating verification scenarios in test mode
     * 
     * <p>In test mode, you can simulate various verification outcomes by adjusting the creditor name in the
     * `creditorBankAccount.accountHolderName` property. This allows you to test all possible Verification
     * of Payee
     * results without needing special properties. The names are case insensitive.
     * 
     * <p>| Account holder name                    | Scenario                                      |
     * Verification result | Suggested name |
     * |----------------------------------------|-----------------------------------------------|---------------------|----------------|
     * | `John Close Match`                     | Name closely matches the bank records          |
     * `close-match`       | `John Match`   |
     * | `John No Match`                        | Name does not match the bank records           |
     * `no-match`          | —              |
     * | `John Unavailable`                     | Verification is not available                  |
     * `not-available`     | —              |
     * | Any other name                         | Default: name matches the bank records         | `match`
     * | —              |
     * 
     * <p>This operation requires Security#organizationAccessToken to be set via the {@code security} builder
     * method when initializing the SDK.
     * 
     * @param idempotencyKey A unique key to ensure idempotent requests. This key should be a UUID v4 string.
     * @param verificationOfPayeeRequest The request body for performing a Verification of Payee (VoP) check. VoP allows you to verify the
     *         account holder name against the records held by the receiving bank before initiating a transfer.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public VerifyPayeeResponse create(
            Optional<String> idempotencyKey, Optional<? extends VerificationOfPayeeRequest> verificationOfPayeeRequest,
            Optional<Options> options) {
        VerifyPayeeRequest request =
            VerifyPayeeRequest
                .builder()
                .idempotencyKey(idempotencyKey)
                .verificationOfPayeeRequest(verificationOfPayeeRequest)
                .build();
        RequestOperation<VerifyPayeeRequest, VerifyPayeeResponse> operation
              = new VerifyPayee.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
