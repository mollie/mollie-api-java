# AccountNumberFormatResponse

The format of the account number.

## Example Usage

```java
import com.mollie.mollie.models.components.AccountNumberFormatResponse;

AccountNumberFormatResponse value = AccountNumberFormatResponse.IBAN;

// Open enum: use .of() to create instances from custom string values
AccountNumberFormatResponse custom = AccountNumberFormatResponse.of("custom_value");
```


## Values

| Name   | Value  |
| ------ | ------ |
| `IBAN` | iban   |