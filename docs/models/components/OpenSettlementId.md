# OpenSettlementId

## Example Usage

```java
import com.mollie.mollie.models.components.OpenSettlementId;

OpenSettlementId value = OpenSettlementId.OPEN;

// Open enum: use .of() to create instances from custom string values
OpenSettlementId custom = OpenSettlementId.of("custom_value");
```


## Values

| Name   | Value  |
| ------ | ------ |
| `OPEN` | open   |